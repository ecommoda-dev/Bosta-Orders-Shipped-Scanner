<div dir="rtl" style="text-align: right;">

# مراجعة عميقة وشاملة — سكانر الشحن + سكانر المرتجعات
### `Bosta-Orders-Shipped-Scanner` (Worker v3.3.0 · الواجهة v3.4) · `Bosta-Orders-Returned-Scanner` (Worker v3.3.0 · الواجهة v3.3.2)

**تاريخ المراجعة:** 07-09-2026
**النطاق:** الـ Workers + الواجهات + قاعدة D1 الحيّة + سلوك الأداة من عين موظف بيستخدمها كل يوم.
**المنهج:** قراءة كاملة للكود سطر بسطر (١٣٣٠ + ١٧٦٩ سطر Worker · ٣٧٨٢ + ٣٠٥٦ سطر واجهة)، **زائد فحص البيانات الحقيقية في D1** — كل نتيجة تحت مكتوب جنبها إن كانت **مؤكَّدة بالأرقام** ولا **كامنة في الكود لسه ما ظهرتش**.

> ⚠️ **مفيش أي تعديل اتعمل على الكود في المراجعة دي.** الملف ده تشخيص وخطة — التنفيذ محتاج موافقتك على الأولويات.

---

## ٠. الملخّص التنفيذي — أهم ٥ حاجات

| # | الحاجة | الحالة | الأداة |
|---|---|---|---|
| **١** | **٩٧٪ من صفوف "الفشل" في سجل المرتجعات كذّابة** — دفعة ناجحة بتفضل مسلّحة والموظف بيضغط تاني | 🔴 **مؤكَّدة بالأرقام** (٥٩ من ٦١) | المرتجعات |
| **٢** | **إلغاء أوردرات لا رجعة فيه بضغطة واحدة — بدون تأكيد وبدون إعادة استعلام** | 🔴 مؤكَّدة من الكود | المرتجعات |
| **٣** | **أداة المرتجعات لسه بتتخطّى الأرقام المعروفة في إعادة الاستعلام** — نفس الباج اللي أداة الشحن وثّقته كـ«أخطر شكل من أشكال النجاح الكاذب» | 🔴 مؤكَّدة من الكود | المرتجعات |
| **٤** | **أداة الشحن مش بتتأكد إن الأوردر الراجع من بحث شوبيفاي هو المطلوب** — حارس موجود في أداة المرتجعات ومش موجود هنا | 🔴 كامنة (كتابة على أوردر غلط) | الشحن |
| **٥** | **رسالة «ده خلاص اتعمل» مش موجودة — وهي السبب الأول للفشل والرفض في الأداتين** | 🟠 مؤكَّدة بالأرقام (٥٩/٦١ · ٦/٧) | الاتنين |

**والخبر الكويس:** كل الإصلاحات الكبيرة اللي اتعملت في v3.2/v3.3 **شغّالة فعلاً ومقيسة** — الفلفلمنت ٣٢٤/٣٢٤، تأكيد الإلغاء ٨٣/٨٣، الاسترجاع ٩١ قطعة. التفاصيل في §٢.

---

## ١. الأدلة من D1 (قاعدة `ecommoda-dev-logs` — قراءة فقط)

```
أداة الشحن   (metafields_change · sourceTool = bosta_orders_shipped_scanner)
  update  · result = null      →  893   (قبل v3.2 — النتيجة ماكانتش بتتسجّل)
  update  · result = success   →  324   (من 03-09 لحد 07-09)
  update  · result = warning   →    0
  update  · result = error     →    0
  rejected· result = rejected  →    7   (من 05-09)
  ─────────────────────────────────────
  إجمالي صفوف update           → 1217   آخر صف: 2026-09-07T13:14:58Z

أداة المرتجعات (sourceTool = bosta_orders_returned_scanner)
  update · result = success    →  552
  update · result = warning    →    2
  update · result = error      →   61   ← 🔴
  ─────────────────────────────────────
  إجمالي صفوف update           →  615   آخر صف: 2026-09-07T11:59:01Z
```

**أكبر دفعة فعلية مقيسة:** ٨٢ أوردر في نافذة عشر ثواني (الشحن، 01-09) · ٦٣ أوردر (المرتجعات، 06-09).
الرقم ده مهم — كل حسابات الأداء تحت مبنية عليه، مش على تخمين.

---

## ٢. حاجات اتفحصت وطلعت **سليمة** (عشان المراجعة تبقى عادلة)

قبل ما ندخل في المشاكل — دي بنود كانت مصدر أعطال حقيقية قبل كده، واتفحصت على بيانات حيّة وطلعت شغّالة:

| البند | الدليل من D1 |
|---|---|
| **الفلفلمنت التلقائي** (أداة الشحن v3.1.0) | **٣٢٤ محاولة / ٣٢٤ نجاح · صفر فشل** — `extra.fulfillment.error` فاضية في كل الصفوف |
| **الانتظار المشروط على الـ Job** (المرتجعات v3.3.0) | **٨٣/٨٣ إلغاء مؤكَّد** — صفر صف أصفر «مش مؤكَّد» بعد النشر. الإصلاح ده كان بيمنع بالظبط إن الأصفر يبقى هو الافتراضي، وهو نجح |
| **دليل استرجاع المخزون بعد الإلغاء** | **٨٣/٨٣ صف معاه `restockedUnits > 0`** — إجمالي ٩١ قطعة رجعت المخزن |
| **استرجاع مسار S2 (`reverseFulfillmentOrderDispose`)** | **٦/٦ مؤكَّد** — ٧ قطع، وصف واحد بس بـ `remainingAfter > 0` |
| **عقد `shopifyGQL`** (فحص HTTP + `data.errors` + `data` فاضية + retry) | مطبّق حرفيًا في الاتنين — وهو اللي منع تكرار عطل 19→23-08 |
| **تأكيد `metafieldsSet` من `owner.id`** | مطبّق في الاتنين |
| **عزل الخطأ لكل chunk في `metafieldsSetBatch`** | مطبّق — chunk فاشل مايوقّعش اللي بعده |

> **الخلاصة:** المشاكل الباقية **مش** في طبقة الاتصال بشوبيفاي/بوسطة — دي اتقفلت كويس. المشاكل دلوقتي في **دورة استخدام الموظف** وفي **الفرق بين الأداتين**.

---

## ٣. النتائج الحرجة 🔴

### 🔴 H1 — الدفعة الناجحة بتفضل «مسلّحة»، والضغطة التانية بتولّد صفوف فشل كذّابة في سجل دائم
**الأداة:** المرتجعات (والشحن بشكل أخف) · **الحالة: مؤكَّدة بالأرقام**

**اللي بيحصل:** بعد ما `startUpdate` يخلص بنجاح في `index.html`:
- `r.selected` **بيفضل `true`** — مفيش سطر واحد بيلغي الاختيار بعد الكتابة
- الـ checkbox بيفضل مفعّل ومعلّم
- الزرار بيفضل مكتوب عليه `↩ تحديث شوبيفاي (63)` بنفس الرقم
- مفيش نافذة ملخّص بتقف قدام الموظف (أداة الشحن عندها واحدة — دي مالهاش)
- التوست بيختفي بعد ٣ ثواني

يعني الشاشة بعد نجاح كامل **شكلها بالظبط زي الشاشة قبل الضغط**. الموظف — اللي بيشتغل في مخزن، وبيبص على الشاشة وبيسيبها — بيضغط تاني.

**الدليل الرقمي:**
```
٥٩ من ٦١ صف فشل (96.7%) على أوردر نفس الأداة نجحت فيه قبل كده
٢٦ منهم خلال أقل من دقيقتين من النجاح

مثال حي — أوردر #53539  (07-09-2026):
   11:58:52.175Z  →  success  (S1 → Returned · الأوردر اتلغى · المخزون رجع)
   11:59:00.359Z  →  error    "الحالة تغيرت قبل التحديث — S1 الحالي: Returned"
                     فرق ٨ ثواني بالظبط
```
كل الدفعة بتاعة 07-09 اتبعتت مرتين. ونفس النمط في 31-08 (٩ أوردرات، ٣ محاولات لكل واحد).

**الأثر الحقيقي:**
1. **١٠٪ من سجل المرتجعات الدائم صفوف حمرا كذّابة** على عمليات نجحت فعلاً — أي تقرير عن «نسبة فشل الأداة» غلط بالكامل.
2. **الموظف بيتعلّم إن الأحمر طبيعي.** ده بالظبط نفس المرض اللي `verifyCancels` v3.3.0 اتكتبت عشانه («الحالة الاستثنائية بقت الافتراضية، والموظف بيتعوّد يتجاهل الأصفر») — بس على الأحمر المرة دي.
3. دفعة كاملة تانية من نداءات شوبيفاي (٦٣ أوردر × نداءين) بتتحرق على الفاضي.
4. الحماية الوحيدة اللي منعت كارثة هي `validateTransition` في الـ Worker. **لو حد وسّع القاعدة يومًا ما ليقبل `Returned` كحالة بداية، الضغطة التانية هتلغي أوردرات تاني.** الحماية دلوقتي بالصدفة، مش بالتصميم.

**الإصلاح المقترح (٤ طبقات — كل واحدة لوحدها بتقفل المشكلة):**
```js
// ① بعد كل نتيجة — الصف اللي اتنفّذ بيتنزع سلاحه
if (res) {
  r.selected = false;
  r.doneInSession = true;          // ما يترشّحش تاني في نفس الجلسة
  const c = document.getElementById(`chk-${i}`); if (c) { c.checked = false; c.disabled = true; }
}
// ... بعد الحلقة
syncSelectAll(); updateUpdateBtn();   // الزرار بيرجع "تحديث شوبيفاي" رمادي

// ② نافذة ملخّص إجبارية (زي أداة الشحن) — بتقف قدام الشاشة لحد ما يقفلها
// ③ في updateUpdateBtn: استبعاد r.doneInSession من العدّ
// ④ في الـ Worker: لو الحالة الحالية == الحالة المستهدفة → مش "error"،
//    ده نوع مستقل (skipped/already) بلون محايد ومايتعدّش في failed
```
**الأولوية: أعلى بند في الملف ده.** التكلفة ~٣٠ سطر.

---

### 🔴 H2 — أداة المرتجعات بتلغي أوردرات لا رجعة فيها بدون أي تأكيد وبدون إعادة استعلام
**الأداة:** المرتجعات · **الحالة: مؤكَّدة من الكود**

`orderCancel` **لا رجعة فيه** — مفيش ميوتيشن بيلغي الإلغاء. الكود نفسه مكتوب فوقه `⚠️ IRREVERSIBLE`.
ومع ذلك `startUpdate` بينفّذ **على طول** أول ما الزرار يتضغط. مفيش:
- ولا نافذة تأكيد
- ولا إعادة استعلام قبل الكتابة (`recheckBeforeUpdate` موجودة في أداة الشحن من v3.3 — **مش موجودة هنا خالص**)

> **ملاحظة موجعة:** الأداة فيها `confirm('تسجيل الخروج؟')` — تأكيد على **تسجيل الخروج**.
> ومفيش تأكيد على **إلغاء ٦٣ أوردر إلغاء نهائي**. الحماية موجودة على الفعل الرخيص ومش موجودة على الغالي.

**السيناريو الواقعي:** الجدول فيه ٦٣ صف، الموظف بيشيل ٥ صفوف بإيده، بيتلهي، بيرجع، بيضغط "تحديث" فاكر إنه لسه ما ضغطش (أو زي H1 — بيضغط تاني). ٦٣ إلغاء نهائي.
سيناريو تاني: بين الاستعلام والضغط عدّت ١٠ دقايق، الـ CS عمل approve لمرتجع، الحالة اتغيّرت. الـ Worker بيمسك دي (`freshMap`) — بس الموظف بيعرف **بعد** ما ضغط، كصف أحمر وسط النتيجة.

**الإصلاح المقترح:**
```js
// ① نافذة تأكيد بتسمّي الأثر بالظبط — مش confirm() عام
const rto = toUpdate.filter(r => r.machine === 'S1').length;
const s2  = toUpdate.length - rto;
if (!await confirmDanger(
  `هتنفّذ على ${toUpdate.length} أوردر:`,
  [ rto ? `🚫 ${rto} أوردر هيتلغوا نهائيًا على شوبيفاي (مفيش رجوع)` : '',
    s2  ? `📦 ${s2} أوردر هيترجّع مخزونهم للمخزن` : '' ]
)) return;

// ② نقل recheckBeforeUpdate من أداة الشحن حرفيًا — هي أصلاً مكتوبة ومجرّبة
```
الحجّة اللي اتكتبت في أداة الشحن («الموظف يشوف الجديد بعينه ويضغط تاني بإرادته») **أقوى هنا عشرين مرة** لأن الفعل هنا ماينفعش يترجع.

---

### 🔴 H3 — أداة المرتجعات لسه بتتخطّى الأرقام المعروفة في إعادة الاستعلام
**الأداة:** المرتجعات · **الحالة: مؤكَّدة من الكود** · `index.html:2192`

```js
const existingTNs = new Set(resultsData.map(r => r.trackingNumber));
const merged = [...resultsData];
for (const r of newEntries) {
  if (!existingTNs.has(r.trackingNumber)) merged.push(r);   // ← الرقم المعروف بيترمى
}
```
الرقم اللي موجود في الجدول **بياخد رد جديد من الـ Worker ويترمى في الزبالة**. الزرار بيدوّر، بيطلع توست «استُعلم عن ٦٣ رقم»، والصف بيفضل عارض حالة اتقرأت من ٢٠ دقيقة.

ده **بالحرف** الباج اللي أداة الشحن أصلحته في v3.3 وكاتبة عنه في `CLAUDE.md` بتاعتها:
> «التخطّي (سلوك ما قبل v3.3) كان بيخلي الزرار يبان إنه استعلم وهو فعليًا بيعرض حالة قديمة اتقرأت من دقايق — **أخطر شكل من أشكال النجاح الكاذب في الأداة دي**.»

الجملة دي اتكتبت عن الأداة اللي **بتكتب ميتافيلد**. وهي لسه حرفيًا موجودة في الأداة اللي **بتلغي أوردرات**.

**الإصلاح:** نقل `refreshRow()` + `mergeLookupResults()` من `Bosta-Orders-Shipped-Scanner/index.html:2442-2477` — الكود جاهز ومجرّب، محتاج بس تعديل الحقول المحفوظة (`updateResult`/`updateDetail` بدل `fulfillment`/`rejectLogged`).

---

### 🔴 H4 — أداة الشحن مش بتتأكد إن الأوردر الراجع من بحث شوبيفاي هو المطلوب فعلاً
**الأداة:** الشحن · **الحالة: كامنة — ما ظهرتش في الإنتاج لحد دلوقتي** · `index.js:520-540`

```js
const safe = name.replace(/[^a-zA-Z0-9\-]/g, '');     // بيتنضّف بصمت
return `o${idx}: orders(first: 1, query: "name:#${safe}") { ... }`;
...
map[name] = node ? { ... } : null;                     // ← بيتحفظ بالاسم الأصلي
```
مشكلتين مع بعض:
1. **التنظيف الصامت:** أي `businessReference` فيه حرف غير `[a-zA-Z0-9-]` (مسافة · نقطة · شرطة سفلية · حرف عربي) بيتحوّل لاسم **مختلف**، والبحث بيتم على الاسم المختلف، **والنتيجة بتتحفظ تحت الاسم الأصلي**.
2. **مفيش مطابقة للاسم الراجع:** مفيش `if (cleanOrderName(node.name) !== name)`.

يعني `orders(first:1, query:"name:#XXXX")` لو رجّع أوردر **مقارب** بدل المطابق، الأداة هتكتب `S1 = Shipped` **وتعمل fulfillment** على الأوردر الغلط، وتسجّله في D1 باسم الأوردر الصح. فشل صامت بأقصى معنى الكلمة.

**أداة المرتجعات عندها الحارسين الاتنين من v3.2.0** (`index.js:604-612`) والتعليق مكتوب صراحة:
> «بحث شوبيفاي `name:#X` ممكن يرجّع أوردر **مقارب** مش مطابق. بنقارن اسم الأوردر الراجع بالمطلوب، وأي اختلاف = "غير موجود" بدل ما نكتب حالة على أوردر تاني.»

**ليه ما ظهرش لحد دلوقتي:** كل الـ `businessReference` الحالية أرقام صافية. ده **حظ في البيانات، مش حماية في الكود** — أول أوردر باسم غير معتاد (استبدال، أوردر يدوي، بادئة جديدة) بيفتح الباب.

**الإصلاح:** نسخ ٨ سطور من `Bosta-Orders-Returned-Scanner/index.js:598-616` حرفيًا.

---

## ٤. النتائج المهمة 🟠

### 🟠 M1 — رسالة «ده خلاص اتعمل» مش موجودة، وهي **السبب الأول** للفشل والرفض في الأداتين
**الحالة: مؤكَّدة بالأرقام**

```
المرتجعات — ٥٩ من ٦١ صف فشل رسالتهم:
  "الحالة تغيرت قبل التحديث — [RTO] S1 لازم يكون Shipped أو In-Return (الحالي: Returned)
   — [مرتجع بعد التسليم] S1 لازم يكون Delivered (الحالي: Returned)، S2 لازم يكون
   Shipped أو In-Return (الحالي: —)، returnStatus لازم يكون IN_PROGRESS (الحالي: NO_RETURN)"

الشحن — ٦ من ٧ صف رفض رسالتهم:
  "S1 ليس Ready (الحالي: Shipped)"
```
**المعنى الحقيقي في الحالتين جملة واحدة: «الأوردر ده خلاص اتعمل قبل كده».**
والأداة بتقوله بـ ٢٠٠ حرف من لغة المهندسين، بلون أحمر يعني «فشل».

الموظف في المخزن **مش بيقرا** «returnStatus لازم يكون IN_PROGRESS». بيشوف أحمر، فبيفتكر إن فيه عطل، فبيروح يسأل، أو — الأسوأ — بيتعوّد ويعدّي.

**الإصلاح المقترح:** حالة رابعة مستقلة في `validateTransition`، قبل أي فحص تاني:
```js
// أداة المرتجعات
if (s1 === S1_STATUS.RETURNED || s2 === S2_STATUS.RETURNED)
  return { valid:false, already:true,
           reason:'✅ الأوردر ده اترجّع خلاص قبل كده — مفيش حاجة مطلوبة' };
// أداة الشحن
if ((isSend && s1 === S1_STATUS.SHIPPED) || (!isSend && s2 === S2_STATUS.SHIPPED))
  return { valid:false, already:true,
           reason:'✅ الأوردر ده اتشحن خلاص قبل كده — مفيش حاجة مطلوبة' };
```
وفي الواجهة: بادج **رمادي/أزرق محايد** مش أحمر، وعدّاد مستقل في الملخّص («خلاص متعمل: ٥٩»)، ومايتعدّش في `failed`.

ده أرخص بند في الملف وأعلى عائد — بيحوّل ٦٥ صف كذّاب في السجل لمعلومة صحيحة، وبيخلّي الأحمر يرجع يعني أحمر.

---

### 🟠 M2 — أداة المرتجعات مالهاش تسجيل للأوردر المرفوض
أداة الشحن ضافت `type = 'rejected'` في v3.3.0 عشان سؤال «ليه الأوردر ده ما اتشحنش؟» يبقى ليه إجابة بعد ما الموظف يقفل الشاشة.
**نفس السؤال في المرتجعات أهم:** «ليه الشحنة دي رجعت المخزن ولسه الأوردر مفتوح؟» — دلوقتي مالهاش أي إجابة في أي مكان. الأوردر بيترفض في `lookup`، الموظف يقفل الشاشة، والأثر بيروح.

**الإصلاح:** نقل `logRejectedItems()` + مسار `rejected[]` من أداة الشحن (`index.js:1250-1330`)، مع نفس الحارسين: السبب يتحسب في الـ Worker وقت التسجيل، و`becameValid` مايتسجّلش مرفوض.
🔴 **مربوط ببند مفتوح:** `type='rejected'` **لسه مش مسجّل في `ecommoda-constants` §7** رغم إن ٧ صفوف اتكتبت بيه فعلًا — لازم يتسجّل قبل ما أداة تانية تكتبه.

---

### 🟠 M3 — أداة الشحن مابتسجّلش صفوف الفشل وقت الكتابة
`handleUpdate` في `index.js:1080-1097`: الصف اللي بيفشل في `validateTransition` وقت الكتابة أو مش موجود على شوبيفاي بياخد `results.push(...)` و**`continue` من غير `writeLog`**.

يعني: أوردر كان متوافق وقت الاستعلام، موظف تاني حرّكه، الكتابة اتوقفت — **صفر أثر في D1**. والحالة دي بالذات (سباق بين موظفين) هي أكتر حاجة محتاجة أثر.
أداة المرتجعات بتسجّلها (`if (rec.executed || rec.error)`), وده الصح.

**الإصلاح:** إضافة `writeLog` بـ `type='rejected'` (أو `type='update'` + `result='error'`) لكل صف بيخرج من الحلقة قبل الكتابة.

---

### 🟠 M4 — حارس التكرار (`seenOrders`) مش موجود في أداة الشحن
**الحالة: كامنة — صفر تكرار في نفس الدفعة لحد دلوقتي (اتفحص في D1)**

أداة المرتجعات ضافت `seenOrders` في v3.3.0 لأن **رقمين تتبع ممكن يرجعوا لنفس الأوردر** (شحنة + شحنة مرتجع). أداة الشحن مافيهاش الحارس ده خالص، والحالة واردة فيها كمان (أوردر اتشحن، رجع، اتشحن تاني).

لو حصل، أربع نتايج مع بعض:
1. مدخلين متطابقين (`ownerId` + `key`) في نفس الـ `metafieldsSet` chunk
2. `perOrderMeta[metaKey]` بيتكتب فوق نفسه → **رقم التتبع الأول بيضيع من السجل**
3. الحلقة `for (const w of toWrite)` بتلف مرتين على نفس المفتاح → **صفّين في D1 لكتابة واحدة** — وده بيضخّم بالظبط العدّاد اللي `CLAUDE.md` معتمدة عليه كخط أساس
4. **`createFulfillment` بيتنادى مرتين** → التانية بتفشل → **تحذير أصفر كاذب** على أوردر فلفلمنته تمّت تمام

النقطة ٤ بالذات هي مصنع الإنذارات الكاذبة اللي الأداتين بيحاربوه في كل حتة.

**الإصلاح:** نفس ١٠ سطور `seenOrders` من `Bosta-Orders-Returned-Scanner/index.js:1535-1545`.

---

### 🟠 M5 — صف سجل واحد بـ `extra` تالف بيوقّع تاب السجل كله في المرتجعات
`index.html:2841`:
```js
const extra = e.extra ? (typeof e.extra === 'string' ? JSON.parse(e.extra) : e.extra) : {};
```
`JSON.parse` **من غير `try/catch`** جوّه `.map()`. صف واحد بـ JSON مقطوع ⇒ الاستثناء بيطلع لـ `fetchLog` ⇒ **تاب السجل كله بيبان "❌ فشل التحميل: Unexpected token…"**، ومفيش أي إشارة لأنهي صف.
أداة الشحن محوّطاها: `try { extra = JSON.parse(e.extra || '{}'); } catch { }` مع تعليق «صف قديم بـ extra تالف».

**الإصلاح:** سطرين.

---

### 🟠 M6 — تصدير النتائج في أداة الشحن بيبلع الحالة الصفرا
`index.html:3742`:
```js
shopify: r.shopifyStatus === 'success' ? 'تم التحديث'
       : r.shopifyStatus === 'error'   ? 'فشل' : '—',
```
صف `warning` (الميتافيلد اتكتب والفلفلمنت فشل) بيتصدّر **`—`** — نفس قيمة صف ما اتحاولش أصلاً.
ده بالظبط باج «تلات حالات اتحوّلت لاتنين» اللي الأداة قافلاه في الجدول وفي النافذة وفي السجل — **ونجا في التصدير**. والتصدير هو اللي بيروح للمدير.
(تصدير أداة المرتجعات مظبوط: `'تم جزئيًا (راجع الملاحظة)'`.)

**الإصلاح:** سطر واحد + عمود «سبب التحذير».

---

### 🟠 M7 — حراسة الأرقام ناقصة في `get_logs` بأداة الشحن
`index.js:799`: `Math.min(parseInt(url.searchParams.get('limit') || '100'), 100)`
`parseInt('abc')` → `NaN` → `Math.min(NaN,100)` → `NaN` يتبعت لـ D1 → خطأ غامض.
أداة المرتجعات أصلحتها في v3.2.0 بـ `Number.isFinite` (`index.js:1191-1195`). فرق مباشر بين الأداتين.

---

### 🟠 M8 — شريط التقدّم كذّاب، والموظف بيبص على شاشة مجمّدة ٢٠-٦٠ ثانية
**الحالة: محسوبة على دفعات حقيقية مقيسة**

الـ `update` نداء **واحد** بيرجع بعد ما كل حاجة تخلص. `progressBar` بيتحرّك في حلقة `for` **بعد** ما الرد وصل — يعني بيقفز من 0% لـ 100% في جزء من الثانية. طول العملية الفعلية الشاشة بتقول `0 / 82`.

**الحساب على أكبر دفعة مقيسة:**
```
أداة الشحن — ٨٢ أوردر:
  1 OAuth + 5 نداءات بحث (82÷20) + 4 metafieldsSet (82÷25) + 82 fulfillmentCreate
  ≈ ٩٢ نداء شوبيفاي متتابع  ≈ ٢٠–٣٥ ثانية

أداة المرتجعات — ٦٣ أوردر (مسار RTO):
  1 OAuth + 4 نداءات بحث + (63 × 2 نداء) + حلقة التحقق (٦ ثواني + قراءات)
  ≈ ١٣٠ نداء متتابع  ≈ ٤٠–٦٠ ثانية
```
دي أطول لحظة في يوم الموظف، وهي اللحظة الوحيدة اللي الأداة بتسكت فيها تمامًا. والسكوت ده **هو** الوقود المباشر لـ H1 (بيضغط تاني فاكر إنها ما اشتغلتش).

**الإصلاح المقترح (بيحل ٣ مشاكل مرة واحدة):** تقسيم الدفعة في الواجهة لمجموعات ١٥-٢٠:
```js
const CHUNK = 15;
for (let i = 0; i < items.length; i += CHUNK) {
  const part = items.slice(i, i + CHUNK);
  const data = await apiPost('update', { employee, items: part, rejected: … });
  applyResults(data);                                    // الجدول بيتلوّن أول بأول
  progressBar.style.width = `${Math.round((i+part.length)/items.length*100)}%`;
}
```
المكسب: تقدّم حقيقي · وقت النداء الواحد بينزل تحت ١٠ ثواني · انقطاع الشبكة بيضيّع مجموعة مش دفعة · والموظف بيشوف الصفوف بتتلوّن فبيبطّل يضغط تاني.

---

### 🟠 M9 — الترتيب في تاب السجل بيرتّب **الصفحة** بس، والشكل بيقول غير كده
`applySort(logRows)` في الأداتين بيرتّب `logRows` = **١٠٠ صف الصفحة الحالية** بس، بينما الـ pagination server-side بترتيب `timestamp DESC`.
يعني «رتّب بالموظف» على سجل فيه ١٢١٧ صف بيرتّب ١٠٠ ويسيب ١١١٧. السهم في الترويسة شكله ترتيب عام.
**الإصلاح:** يا إما تسمية صريحة («ترتيب داخل الصفحة») أو `ORDER BY` server-side مع whitelist للأعمدة.

---

### 🟠 M10 — فلتر «النتيجة» غايب من تاب سجل أداة الشحن
أداة المرتجعات عندها فلاتر `results` (success/warning/error) و`machines` (S1/S2) server-side.
أداة الشحن عندها الموظف والنوع بس — يعني **«وريني كل اللي فيه تحذير»** (أي: أنهي أوردرات فلفلمنتها فشل) **مستحيلة** فيها. وده أهم سؤال تشغيلي في الأداة دي.
**الإصلاح:** نقل الفرعين من `buildLogFilterSQL` بتاعة المرتجعات (`index.js:359-366`) + الفلتر في الواجهة.

---

## ٥. تحسينات ومخاطر كامنة 🟡

| # | البند | الأداة | التفاصيل |
|---|---|---|---|
| **L1** | **منطق تأخير السكانر ميت** | الاتنين | `const gap = len>3 ? 80 : 400; setTimeout(handleScan, gap<80 ? 150 : 400)` — `gap` قيمته ٨٠ أو ٤٠٠، فـ `gap<80` **عمرها ما بتتحقق**: التأخير **دايمًا ٤٠٠ms**. أثرين: (أ) ٤٠٠ms × ٨٢ سكان ≈ **٣٣ ثانية انتظار ميت** في الدفعة، (ب) لو الموظف سكن أسرع من ٤٠٠ms، الـ input مابيتمسحش والباركودين **بيتلزقوا في رقم واحد** يعدّي `replace(/\D/g,'')` من غير ما يشتكي |
| **L2** | **مفيش تحقق من طول رقم التتبع** | الاتنين | `num.replace(/\D/g,'')` بيقبل أي طول — رقم ملزوق (L1) أو باركود غلط بيتضاف عادي. حارس `length` بين ٨ و١٣ بيمسك الاتنين |
| **L3** | **مفيش حد أقصى لحجم الدفعة على السيرفر** | الاتنين | `trackingNumbers[]` و`items[]` بلا سقف. لصق ٥٠٠ رقم = نداء Worker بخمس دقايق |
| **L4** | **مفيش timeout على `fetch` في الواجهة** | الاتنين | Worker واقف = spinner للأبد. `AbortController` بـ ٩٠ ثانية |
| **L5** | **صفر escaping قبل `innerHTML`** | الاتنين | `rejectReason` · `warning` · `notes` · `businessRef` · `e.message` كلها بتتحقن مباشرة. مش ثغرة اختراق (البيانات من APIs موثوقة) لكن `<` واحد في رسالة خطأ بيكسر الجدول. دالة `esc()` واحدة |
| **L6** | **اسم ملف التصدير بتوقيت UTC** | الاتنين | `new Date().toISOString().slice(0,10)` والباقي بتوقيت القاهرة — تصدير الساعة ١ صباحًا بيتسمّى بتاريخ إمبارح |
| **L7** | **التاريخ في XLSX نص مش date** | الاتنين | Excel مش هيرتّب ولا يفلتر بيه. `ws.getCell().value = new Date(...)` + `numFmt` |
| **L8** | **أسماء الموظفين خام في سجل المرتجعات** | المرتجعات | `row.emp` بيعرض `ahmed_ibrahim`. أداة الشحن عندها `employeeDisplayName()` — تتنقل |
| **L9** | **`dispositionType` مثبّت على `RESTOCKED`** | المرتجعات | **بند تشغيلي حقيقي:** كل قطعة راجعة بترجع مخزون **صالح للبيع** — مفيش مسار للقطعة التالفة. شوبيفاي بتدعم `DAMAGED`/`DISPOSED`/`PROCESSING_REQUIRED`. المخزن محتاج الاختيار ده، وإلا القطع التالفة بتتباع تاني |
| **L10** | **`orderCancel(reason:'CUSTOMER')` لكل RTO** | المرتجعات | أي تقرير مستقبلي عن أسباب الإلغاء هيقول «العميل طلب» على شحنات العميل ما استلمهاش أصلًا. `DECLINED` أدق |
| **L11** | **رسائل شوبيفاي الإنجليزية بتوصل خام للموظف** | المرتجعات | ظهرت فعلاً: `"Cannot cancel an order that has outstanding fulfillments (INVALID)"` · `"Cannot cancel an order that has already been canceled"`. ماب صغير للعربي |
| **L12** | **`read_all_orders` مش في `REQUIRED_SCOPES` ولا بيتفحص** | الاتنين | شوبيفاي بتخفي الأوردرات **الأقدم من ٦٠ يوم** عن أي تطبيق مالوش الصلاحية دي. مرتجع بطيء بعد شهرين هيطلع «الأوردر غير موجود على شوبيفاي» — رسالة كاذبة تمامًا. **الأوردرات الحالية أحدث من ٣٠ يوم فمازالتش ظهرت.** ضيف الصلاحية لفحص `diag` عشان تعرف الإجابة بدل ما تكتشفها بعطل |
| **L13** | **مفيش pagination في قراءة المرتجعات** | المرتجعات | `returns(first:20)` · `reverseFulfillmentOrders(first:20)` · `lineItems(first:100)` — قصّ صامت على أوردر كبير |
| **L14** | **بانر إعادة الاستعلام بيقول كلام مش دايمًا صح** | الشحن | «الصفوف دي اتشالت من الاختيار» — صف اتغيّر ولسه `valid` بيفضل مختار في `refreshRow`. نادر بس البانر بيكذب |
| **L15** | **الجدول بيفضل بالحالة القديمة بعد `update` ناجح** | الاتنين | `s1`/`s2` في الصف لسه القديمة بعد الكتابة. تحديثها من رد الـ Worker مباشرة |
| **L16** | **`URL.createObjectURL` من غير `revoke`** | الشحن | تسريب ذاكرة بسيط. المرتجعات بتعمل revoke |
| **L17** | **٨٩٣ صف (٧٣٪) في سجل الشحن نتيجتهم «—»** | الشحن | متوقّع وموثّق (قبل v3.2)، بس تنبيه صغير فوق الجدول («صفوف ما قبل 03-09 مالهاش نتيجة مسجّلة») بيوفّر السؤال |

---

## ٦. الأمان 🔐

| البند | التقييم |
|---|---|
| **CORS** | ✅ ممتاز. `ALLOWED_ORIGINS` صارمة + الافتراضي في `json()` أول أصل مسموح مش `*` — دي حماية من النسيان مش من القرار، وهي الشكل الصح |
| **الأسرار في `diag`** | ✅ أسماء وأطوال بس. والطول بيكشف المسافة المخفية — فكرة ممتازة |
| **`WORKER_SECRET` عند العميل** | ⚠️ **قيد معماري:** السر مخزّن في `localStorage` عند كل موظف. أي موظف يقدر يفتح DevTools وينادي `?action=update` مباشرة — **بيتخطّى الـ PIN وبيكتب أي `employee` هو عايزه في السجل**. يعني نسبة الأوردرات لموظف في السجل **مش دليل**. الحل النهائي توكن لكل موظف من `verify_employee`، وده قرار على مستوى الستاك مش الأداة |
| **PIN بـ plaintext + بلا حد للمحاولات** | ⚠️ ٤ أرقام = ١٠ آلاف احتمال، ومفيش قفل ولا تأخير. الكود جوّه بلوك `§SHARED` المكتوب فوقه «انسخه حرفيًا» ⇒ **الإصلاح مكانه `ecommoda-worker-builder` مش الأداتين**. توصية: hash + عدّاد محاولات + قفل ٥ دقايق |
| **SQL Injection** | ✅ كل حاجة `.bind()`. الوحيدة اللي بتتلزق في نص هي أسماء الأوردرات في الـ GraphQL aliases — وهي منضّفة (بس شوف H4 للنتيجة الجانبية) |
| **`get_employees` بيرجّع القايمة** | ✅ خلف `WORKER_SECRET` — مقبول |

---

## ٧. فجوات التطابق بين الأداتين

الأداتين شقيقتين وبيتطوروا بالتبادل — واللي بيحصل إن الإصلاح بيتعمل في واحدة وبينسى في التانية. الجدول ده هو **الأهم في الملف** لأنه بيوصف السبب الجذري.

| القدرة | الشحن | المرتجعات | مين الناقص |
|---|:---:|:---:|---|
| مطابقة اسم الأوردر الراجع من البحث | ❌ | ✅ v3.2.0 | **الشحن** (H4) |
| رفض الاسم غير الصالح بدل تنظيفه بصمت | ❌ | ✅ v3.2.0 | **الشحن** (H4) |
| حارس تكرار الأوردر في الدفعة (`seenOrders`) | ❌ | ✅ v3.3.0 | **الشحن** (M4) |
| حراسة `parseInt` في `get_logs` | ❌ | ✅ v3.2.0 | **الشحن** (M7) |
| تسجيل صفوف الفشل وقت الكتابة | ❌ | ✅ | **الشحن** (M3) |
| فلاتر سجل بالنتيجة/الماكينة | ❌ | ✅ | **الشحن** (M10) |
| `try/catch` حوالين `JSON.parse` في السجل | ✅ | ❌ | **المرتجعات** (M5) |
| اسم الموظف المعروض في السجل | ✅ | ❌ | **المرتجعات** (L8) |
| إعادة استعلام حقيقية (مش تخطّي) | ✅ v3.3 | ❌ | **المرتجعات** (H3) |
| إعادة استعلام إجبارية قبل الكتابة | ✅ v3.3 | ❌ | **المرتجعات** (H2) |
| نافذة ملخّص بعد الدفعة | ✅ v3.4 | ❌ | **المرتجعات** (H1) |
| تسجيل الأوردر المرفوض (`rejected`) | ✅ v3.3.0 | ❌ | **المرتجعات** (M2) |
| نزع سلاح الصفوف بعد النجاح | ❌ | ❌ | **الاتنين** (H1) |
| تأكيد قبل فعل لا رجعة فيه | لا ينطبق | ❌ | **المرتجعات** (H2) |
| تصدير النتائج بيحترم الحالة الصفرا | ❌ | ✅ | **الشحن** (M6) |
| رسالة «خلاص اتعمل» | ❌ | ❌ | **الاتنين** (M1) |
| تأخير السكانر شغّال | ❌ | ❌ | **الاتنين** (L1) |

> **التوصية المعمارية:** ١٧ صف في الجدول ده كلهم أعراض لسبب واحد — **كود مشترك متكرر في ريبوهين**. نفس العيلة اللي `CLAUDE.md` بتاعة الشحن محذّرة منها في §BOSTA مع `Orders-Packing-Checker`.
> الخطوة الصح: ملف مشترك واحد (`shared/scanner-core.js` أو Skill بتولّد الاتنين) فيه: `shopifyGQL` · `fetchShopifyOrdersByNames` بحارسينها · `cleanOrderName` · `bostaSearch`/`extractDeliveries` · `buildLogFilterSQL` · بلوك `§SHARED` · ونمط السكانر في الواجهة.
> من غيره، أي إصلاح تحت هيتنسى في ريبو تالت السنة الجاية.

---

## ٨. الأداء والحجم

| المقياس | الشحن (٨٢ أوردر) | المرتجعات (٦٣ أوردر) |
|---|---|---|
| نداءات شوبيفاي المتتابعة | ≈ ٩٢ | ≈ ١٣٠ |
| الوقت التقريبي | ٢٠–٣٥ ثانية | ٤٠–٦٠ ثانية |
| حد الـ subrequests في Cloudflare | تحت السقف — بس الهامش بيضيق | تحت السقف |
| نداءات ممكن تتوازى | `fulfillmentCreate` (٨٢ نداء مستقل تمامًا) | `cancelOrder` (مستقل لكل أوردر) |

**توصيات مرتبة:**
1. **تقسيم الدفعة في الواجهة** (M8) — أرخص وأكبر مكسب، وبيحل H1 كمان.
2. **توازي محدود في الـ Worker:** `fulfillmentCreate` في أداة الشحن ٨٢ نداء مستقل بيتنفّذوا واحد ورا التاني. مجموعات من ٥ بالتوازي بتنزّل الوقت للربع. (خلي بالك من `THROTTLED` — `shopifyGQL` أصلاً بتعيد المحاولة.)
3. **سقف على حجم الدفعة** (L3) — ٢٠٠ رقم كحد أقصى برسالة واضحة.
4. **`Build watch paths` في أداة الشحن لسه `*`** — بند مفتوح في `CLAUDE.md`. تضييقها لـ `index.js` + `wrangler.toml` (chip منفصل لكل واحد، زي المرتجعات) بيمنع نشر Worker على تعديل `.md`.

---

## ٩. بنود توثيق تتقفل أو تتصحّح

### ✅ بند ١٠ — «خط الأساس الحي» في أداة الشحن: **يتقفل دلوقتي**
```
قبل النقل : 893 صف · آخر واحد 2026-09-02T11:35:43Z
بعد النقل : 1217 صف · آخر واحد 2026-09-07T13:14:58Z   (+324 صف · صفر فشل · صفر تحذير)
```
الأداة بتكتب من الريبو فعلاً، والعدّاد بيزيد. البند ده مالوش داعي يفضل مفتوح.

### ✅ خط أساس المرتجعات: **يتقفل**
```
قبل النقل : 123 صف · آخر واحد 2026-08-26T12:51:35Z
بعد النقل : 615 صف · آخر واحد 2026-09-07T11:59:01Z
```

### ⚠️ تصحيح مطلوب في استعلام خط الأساس بتاع المرتجعات
الاستعلام المكتوب في `CLAUDE.md` بيعدّ `type='update'` — و**ده شامل ٦١ صف فشل**. يعني بيقيس **المحاولات مش النجاح**، والرقم الحقيقي للنجاح **٥٥٢** مش ٦١٥.
أداة الشحن حلّت المشكلة دي بفصل `rejected` في نوع مستقل (وموثّقة صراحةً: «عدّاد الكتابة الفعلية يفضل نضيف»). أداة المرتجعات بتحط الفشل جوّه `update`.
**التصحيح:** `... AND json_extract(extra,'$.result') = 'success'` — أو تنفيذ M1 اللي بيطلّع «خلاص متعمل» بره `error` أصلاً.

### 🔴 `type = 'rejected'` لسه مش مسجّل في `ecommoda-constants` §7
٧ صفوف اتكتبت بالقيمة دي فعلاً (05-09 → 06-09). القاعدة في المهارة: التسجيل **قبل** أول `writeLog`. البند ده مفتوح ومطلوب من أحمد.

### 📌 معلومة اتأكدت — الفلفلمنت مش بيمنع الإلغاء
كنت شاكك إن `orderCancel` هيفشل على أوردرات أداة الشحن عملت لها fulfillment. **الفحص نفى ده:** ٨٣/٨٣ إلغاء نجح واتأكد. Shopify بتمنع الإلغاء بس لما يكون فيه fulfillment **معلّق** — حالة ظهرت مرة واحدة بس (29-08).

---

## ١٠. خارطة الطريق المقترحة

### الدفعة الأولى — «وقّف الكذب في السجل» (~نص يوم)
1. **H1** — نزع سلاح الصفوف بعد النجاح + نافذة ملخّص في المرتجعات
2. **M1** — حالة «خلاص اتعمل» مستقلة في الأداتين بلون محايد
3. **M5** — `try/catch` حوالين `JSON.parse` في سجل المرتجعات

> بعد الدفعة دي: صفوف الفشل الحقيقية في سجل المرتجعات بتنزل من ٦١ لـ **٢**.

### الدفعة التانية — «اقفل الأفعال اللي مالهاش رجعة» (~يوم)
4. **H2** — تأكيد + `recheckBeforeUpdate` في المرتجعات
5. **H3** — إعادة استعلام حقيقية (`refreshRow`/`mergeLookupResults`) في المرتجعات
6. **H4** — مطابقة اسم الأوردر + رفض الاسم غير الصالح في الشحن
7. **M4** — `seenOrders` في الشحن

### الدفعة التالتة — «الوضوح والأثر» (~يوم)
8. **M8** — تقسيم الدفعة + تقدّم حقيقي (الاتنين)
9. **M2** + **M3** — تسجيل المرفوض والفاشل في المرتجعات والشحن
10. **M6** · **M7** · **M10** — التصدير والفلاتر والحراسات
11. **L1** + **L2** — تأخير السكانر وحارس الطول

### الدفعة الرابعة — قرارات محتاجة رأيك
12. **L9** — مسار القطعة التالفة (`DAMAGED`) في المرتجعات — **قرار تشغيلي**
13. **L12** — إضافة `read_all_orders` لفحص `diag` — **قرار صلاحيات**
14. **L11/M9** — توكن لكل موظف + ترتيب server-side — **قرار على مستوى الستاك**
15. **الكود المشترك** — ملف/مهارة واحدة تولّد الجزء المتكرر في الريبوهين (+ `Orders-Packing-Checker`)

---

## ١١. ملحق — استعلامات D1 المستخدمة في المراجعة

```sql
-- توزيع النتائج لكل أداة
SELECT json_extract(extra,'$.sourceTool') src, type,
       json_extract(extra,'$.result') res, COUNT(*) n,
       MIN(timestamp) first_ts, MAX(timestamp) last_ts
FROM logs WHERE tool='metafields_change'
  AND (extra LIKE '%bosta_orders_shipped_scanner%'
    OR extra LIKE '%bosta_orders_returned_scanner%')
GROUP BY src, type, res;

-- الدليل على H1: صفوف الفشل اللي على أوردر نجح قبلها
SELECT COUNT(*) FROM logs e
WHERE e.tool='metafields_change'
  AND e.extra LIKE '%bosta_orders_returned_scanner%'
  AND json_extract(e.extra,'$.result')='error'
  AND EXISTS (SELECT 1 FROM logs s
              WHERE s.tool='metafields_change'
                AND s.extra LIKE '%bosta_orders_returned_scanner%'
                AND json_extract(s.extra,'$.result')='success'
                AND s.order_name=e.order_name AND s.timestamp<e.timestamp);
-- → 59 من 61

-- صحة الفلفلمنت (أداة الشحن)
SELECT json_extract(extra,'$.fulfillment.attempted') attempted, COUNT(*) n,
       SUM(COALESCE(json_extract(extra,'$.fulfillment.created'),0)) created,
       SUM(CASE WHEN json_extract(extra,'$.fulfillment.error') IS NOT NULL THEN 1 ELSE 0 END) errs
FROM logs WHERE tool='metafields_change'
  AND extra LIKE '%bosta_orders_shipped_scanner%'
  AND timestamp>'2026-09-03' GROUP BY attempted;
-- → 324 / 324 / 0

-- صحة الإلغاء والاسترجاع (أداة المرتجعات)
SELECT json_extract(extra,'$.machine') machine, COUNT(*) n,
       SUM(CASE WHEN json_extract(extra,'$.cancel.verified')=1 THEN 1 ELSE 0 END) verified,
       SUM(COALESCE(json_extract(extra,'$.cancel.restockedUnits'),0)) units
FROM logs WHERE tool='metafields_change'
  AND extra LIKE '%bosta_orders_returned_scanner%'
  AND json_extract(extra,'$.result')='success'
  AND timestamp>'2026-09-05' GROUP BY machine;
-- → S1: 83/83 مؤكَّد · 91 قطعة

-- أكبر دفعة فعلية
SELECT src, substr(timestamp,1,15) batch, COUNT(*) rows_in_batch FROM (…)
GROUP BY src, batch ORDER BY rows_in_batch DESC;
-- → 82 (الشحن) · 63 (المرتجعات)
```

---

## ١٢. الخلاصة

الأداتين مبنيّتين على مبدأ صح ومكتوب بوضوح في كودهم: **«مفيش نجاح من غير دليل»**. والمبدأ ده اتنفّذ **ممتاز** على طبقة الاتصال الخارجي — `shopifyGQL`، تأكيد الميوتيشن من ردها، الانتظار المشروط، فصل «تعذّر الاستعلام» عن «غير موجود». البيانات الحيّة بتأكد إن الطبقة دي شغّالة.

المشكلة الباقية إن **نفس المبدأ ما اتطبّقش على الطبقة اللي الموظف بيلمسها**:
- شاشة بعد نجاح كامل شكلها زي شاشة قبل الضغط ⇒ **٩٧٪ من صفوف الفشل كذّابة**
- فعل لا رجعة فيه بضغطة واحدة من غير تأكيد، بينما تسجيل الخروج ليه تأكيد
- أهم رسالة في الأداة («ده خلاص اتعمل») مكتوبة بلغة مهندسين بلون أحمر

والسبب الجذري لنص النتايج دي هو نفسه: **كود شقيق متكرر في ريبوهين، والإصلاح بيتعمل في واحد وبيتنسى في التاني** — ١٧ صف في جدول §٧ بيثبتوا ده. الحل الطويل هو المصدر المشترك؛ الحل القصير هو الدفعتين الأولانيين في §١٠.

---

**آخر تحديث:** 07-09-2026 · مراجعة مبنية على قراءة كاملة للكود + فحص D1 الحيّ
**كل الأرقام في الملف ده من قاعدة `ecommoda-dev-logs` وقت المراجعة — قراءة فقط، صفر كتابة.**

</div>
