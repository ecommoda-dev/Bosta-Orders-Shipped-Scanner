<div dir="rtl" style="text-align: right;">

# تغذية راجعة للمهارات — من مراجعة سكانري بوسطة

![version](https://img.shields.io/badge/version-v1.0.0-blue)

**التاريخ:** 08-09-2026
**المصدر:** مراجعة 07-09-2026 لـ `Bosta-Orders-Shipped-Scanner` و`Bosta-Orders-Returned-Scanner` (`REVIEW-2026-09-07.md`) وتنفيذ الإصلاحات (Worker v3.4.0 · الواجهتين v3.5/v3.4).
**السؤال:** أنهي إصلاح من اللي اتعمل النهاردة **سببه المهارة مش الأداة** — يعني هيتكرر في أي أداة جاية لو المهارة ما اتغيّرتش؟

> ⚠️ **الملف ده مقترحات لمراجعتك — مفيش أي مهارة اتعدّلت.** التعديل الفعلي
> لازم يعدّي على `ecommoda-skill-versioning` (رفع إصدار + بند CHANGELOG مصنَّف
> + سطر `🔴 معلّقة` في الأدوات المتأثرة).

---

## ٠. الخلاصة في سطرين

من ~٣٥ إصلاح اتعمل النهاردة، **١٩ بند سببه المهارة** موزّعين على ٥ مهارات.
**وأخطر تلاتة مش "نقص في المهارة" — دول أخطاء مكتوبة جوّه المهارة نفسها والأدوات نسختها حرفيًا:**

| # | البند | المكان بالظبط |
|---|---|---|
| **١** | **نمط السكانر فيه فرع ميّت** — الشرط عمره ما بيتحقق، فالتأخير دايمًا ٤٠٠ms والمسار السريع مش موجود | `ecommoda-html-builder/SKILL.md:819-820` |
| **٢** | **`parseInt` بلا حراسة** بيوصل `NaN` لـ D1 | `ecommoda-worker-builder/references/shared-functions.md:264-265` |
| **٣** | **`ORDER BY timestamp DESC` مكتوب حرفيًا** — مفيش دعم ترتيب أصلًا مع pagination server-side | `shared-functions.md:163, 188` |

البند ٢ له دلالة خاصة: **أداة المرتجعات أصلحته محليًا في v3.2.0 والمهارة ما اتغيّرتش** — فأداة الشحن اللي اتبنت بعدها **ورثت الباج تاني**. ده الدليل العملي على إن إصلاح الأداة من غير إصلاح المهارة بيرجع.

---

## ١. القاعدة اللي حكمت بيها

مش كل إصلاح يستاهل تعديل مهارة. البند بيتحوّل لمهارة **بشرطين مع بعض**:

| الشرط | المعنى |
|---|---|
| **(أ) السبب في المهارة** | الغلط حصل لأن المهارة سكتت عنه، أو — الأسوأ — لأنها بتقوله |
| **(ب) قابل للتكرار** | أي أداة جاية من نفس المهارة معرّضة لنفس الغلط |

**اللي فشل في الشرطين اتساب بره** — قايمة صريحة في §٧ عشان مايتقالش إني نسيتها.

**التصنيف** حسب `ecommoda-skill-versioning`:
🔴 **كاسر** = كود كان صح بقى غلط · 🟠 **مُستحسن** = بند جديد · 🟡 **تحريري** = توضيح

---

## ٢. `ecommoda-html-builder`  ·  ٩ بنود  ·  المقترح: **v7.0.0**

### 🔴 H-1 — نمط السكانر في المهارة فيه فرع ميّت (الأهم في الملف كله)

**النص الحالي (`SKILL.md:817-822`):**

```javascript
inputEl.addEventListener('input', () => {
  clearTimeout(scanTimer);
  const gap = inputEl.value.length > 3 ? 80 : 400; // scanner bursts <80ms apart
  scanTimer = setTimeout(handleScan, gap < 80 ? 150 : 400);
  //                                  ↑ 150ms after scanner burst ends
  //                                             ↑ 400ms grace for manual typing
});
```

**الغلط:** `gap` قيمته **٨٠ أو ٤٠٠**، يعني `gap < 80` **عمرها ما بتتحقق**.
التأخير **دايمًا ٤٠٠ms**، والقيمة `150` في الكود **كود ميت**. والتعليق بيوصف
سلوك مش موجود — وده اللي خلّى الباج يعيش: اللي بيقرا بيصدّق التعليق.

**الأثر المقيس (دفعة ٨٢ أوردر — رقم حقيقي من D1):**
- ٤٠٠ms × ٨٢ = **≈٣٣ ثانية انتظار ميت** في الدفعة الواحدة.
- **والأخطر:** لو الموظف سكن أسرع من ٤٠٠ms، المؤقّت بيتصفّر والـ input
  **مابيتمسحش** (المسح جوّه `handleScan`) → **الباركودين بيتلزقوا في رقم واحد**،
  ويعدّي `replace(/\D/g,'')` من غير أي شكوى، ويرجع "غير موجود ببوسطة" —
  **رسالة كاذبة على شحنتين موجودتين**.

**ليه مهارة:** الكود ده في `SKILL.md` **بس** (مش في template)، فكل أداة فيها
سكانر نسخته من هناك. الأداتين كانوا بيه حرفيًا.

**النص المقترح:**

```javascript
// ✅ CORRECT — listener sets timeout only; handleScan reads + clears value
let scanTimer;
inputEl.addEventListener('input', () => {
  clearTimeout(scanTimer);
  // دفعة السكانر بتيجي أسرع من 80ms بين الحروف: بعد 3 حروف نعتبرها سكانر
  // ونستنى 150ms بس بعد آخر حرف. الكتابة اليدوية بتاخد المهلة الكاملة.
  const isBurst = inputEl.value.length > 3;
  scanTimer = setTimeout(handleScan, isBurst ? 150 : 400);
});
```

> 🔴 **البند ده كان مكتوب بشرط وسيط (`gap`) عمره ما بيتحقق** — التأخير كان
> دايمًا 400ms والمسار السريع ما اشتغلش ولا مرة في أي أداة في الستاك.
> **متعيدهاش لشكل بمتغيّر وسيط**: الشرط لازم يبقى مقروء ومباشر.

**والبند التوأم — حارس المعقولية (جديد):**

```javascript
// ─── §SCAN::plausibility — حارس إلزامي على أي قيمة مسحوبة ───
// السكانر بيغلط بطريقتين صامتتين: باركودين بيتلزقوا في قيمة واحدة، وباركود
// بيتقرا نص قراية. الاتنين بيعدّوا أي تنضيف بسيط (strip غير الأرقام) ويوصلوا
// للـ API كقيمة "صالحة" — فترجع "غير موجود" وهي رسالة كاذبة.
// أي إدخال سكانر لازم يعدّي على فحص طول/شكل قبل ما يدخل القايمة.
const TN_MIN_LEN = 8, TN_MAX_LEN = 13;   // حدود بوسطة — عدّلها حسب مصدر الباركود

if (clean.length < TN_MIN_LEN || clean.length > TN_MAX_LEN) {
  showToast(
    `الرقم "${clean}" طوله ${clean.length} رقم — المتوقّع بين ${TN_MIN_LEN} و${TN_MAX_LEN}. `
    + (clean.length > TN_MAX_LEN ? 'غالبًا اتسكن باركودين ورا بعض — اسكنهم تاني واحد واحد.'
                                 : 'غالبًا الباركود اتقرا ناقص — اسكنه تاني.'),
    'warning', 6000);
  playBeep('warn'); return;
}
```

**التصنيف:** 🔴 كاسر (كود المهارة نفسه غلط) → **MAJOR**

---

### 🔴 H-2 — «نزع سلاح الصف بعد التنفيذ» غير موجود في المهارة خالص

**الدليل الرقمي (D1، أداة المرتجعات):**

```
٥٩ من ٦١ صف فشل (٩٧٪) كانوا على أوردرات الأداة نجحت فيها قبل كده
٢٦ منهم خلال أقل من دقيقتين
#53539 → success 11:58:52.175Z · error 11:59:00.359Z   (فرق ٨ ثواني)
```

**السبب:** بعد دفعة ناجحة بالكامل، الصفوف بتفضل `selected`، والزرار بيفضل مكتوب
عليه نفس العدد، والتوست بيختفي في ٣ ثواني. **الشاشة بعد النجاح شكلها بالظبط زي
قبل الضغط** — فالموظف في مخزن بيبص بعيد وبيرجع، وبيضغط تاني.

**ليه مهارة:** Step 3C بيغطّي **عرض** النتيجة (٣ حالات) بالكامل، ومفيش **ولا
كلمة** عن حالة **الاختيار** بعد الكتابة. وده بينطبق على **أي أداة فيها جدول
بـ checkboxes وزرار تنفيذ دفعة** — يعني أغلب الستاك.

**النص المقترح — بند جديد في Step 3C:**

> #### الصف اللي اتنفّذ بيتنزع سلاحه — إلزامي
>
> بعد ما أي صف يتنفّذ، **في نفس اللحظة**:
>
> ```javascript
> r.selected      = false;
> r.doneInSession = true;                       // بتتحافظ عليها عبر إعادة الاستعلام
> chk.checked = false; chk.disabled = true;
> // ... بعد الحلقة
> syncSelectAll(); updateUpdateBtn();           // الزرار يرجع رمادي
> ```
>
> **وتلات قواعد مصاحبة:**
> 1. `toggleSelectAll` و`updateUpdateBtn` **يستثنوا `doneInSession`** — "تحديد
>    الكل" بعد دفعة ناجحة كان بيرجّع نفس الأوردرات للاختيار.
> 2. العلامة **بتتحافظ عليها في `refreshRow`** — دي **فعل حصل**، مش حالة
>    بتتقرا من الـ API. إعادة الاستعلام مابتنزعهاش.
> 3. **الاستثناء الوحيد: صف `error`** بيفضل قابل لإعادة المحاولة (ممكن يكون
>    خطأ شبكة عابر) — والحماية عليه هي إعادة الاستعلام الإجبارية قبل الكتابة.
>
> ⚠️ **نافذة ملخّص إجبارية** بعد أي دفعة كتابة. التوست بيختفي في ٣ ثواني، وفي
> مخزن دي مدة كافية إن النتيجة تضيع بالكامل.
>
> 🔴 **الحماية الوحيدة اللي منعت كارثة في الحالة دي كانت `validateTransition`
> (الأوردر بقى `Returned` فاترفض).** يعني الحماية كانت **بالصدفة مش بالتصميم** —
> لو حد وسّع قاعدة الانتقال يومًا ما، الضغطة التانية كانت هتلغي أوردرات تاني.

**التصنيف:** 🟠 مُستحسن (بند جديد) → **MINOR** — بس أثره أكبر من أي كاسر في الملف ده

---

### 🔴 H-3 — تأكيد إلزامي قبل أي فعل لا رجعة فيه

**الدليل:** أداة المرتجعات كان فيها `confirm('تسجيل الخروج؟')` — تأكيد على
**تسجيل الخروج** — **ومفيش أي تأكيد على إلغاء ٦٣ أوردر إلغاء نهائي**.
الحماية كانت على الفعل الرخيص ومش موجودة على الغالي.

**ليه مهارة:** `modals-patterns.md` بيذكر "confirm dialogs" كشكل، وStandards #38
ضاف `.btn-red` "لنافذة بحالة سلبية/نهائية" — بس **مفيش قاعدة بتقول إمتى النافذة
دي إلزامية**. فالنتيجة إن الشكل موجود والقرار متروك للصدفة.

**النص المقترح:**

> #### تأكيد قبل الفعل اللي مالوش رجعة — إلزامي
>
> أي فعل **مفيش ميوتيشن بيرجّعه** (إلغاء أوردر · حذف · refund · تحرير مخزون)
> لازم يعدّي على نافذة تأكيد **بتسمّي الأثر بالأرقام**، مش بتسأل "متأكد؟".
>
> ```javascript
> const ok = await askConfirm({
>   header: '⚠️ تأكيد التنفيذ',
>   lead:   `هتنفّذ على ${n} أوردر:`,
>   lines: [
>     { text: `🚫 ${rto} أوردر هيتلغي نهائيًا — الإلغاء مفيش رجوع فيه` },
>     { text: `📦 ${s2} أوردر هيترجّع مخزونه`, soft: true },
>   ],
>   okLabel: `تأكيد — نفّذ على ${n} أوردر`,
> });
> if (!ok) return;
> ```
>
> **زرار التأكيد `.btn-red`** (Standards #38)، وزرار الإلغاء هو الافتراضي.
> ⚠️ **"متأكد؟" مش تأكيد** — الموظف بيضغط "أيوه" آليًا. اللي بيوقّفه هو **الرقم**.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟠 H-4 — شريط التقدّم لازم يعبّر عن شغل حصل فعلاً

**الدليل:** الأداتين كانوا بينادوا `update` **نداء واحد** للدفعة كلها، والشريط
بيتحرّك في حلقة **بعد** ما الرد وصل — يعني بيقفز من 0% لـ 100% في جزء من الثانية.
طول العملية الفعلية (٨٢ أوردر ≈ ٩٢ نداء شوبيفاي متتابع ≈ ٢٠-٣٥ ثانية) الشاشة
بتقول `0 / 82`. **والسكوت ده هو الوقود المباشر للضغطة التانية (H-2).**

**النص المقترح:**

> #### التقدّم مايتزوّرش — وأي عملية أطول من ٥ ثواني بتتقسّم
>
> ⛔ **ممنوع** تحريك شريط التقدّم في حلقة بعد وصول الرد — ده **تقدّم كذّاب**.
> ✅ أي عملية دفعة متوقّع تعدّي ٥ ثواني **بتتقسّم في الواجهة** والشريط بيتحرّك
> بعد كل مجموعة، والجدول بيتلوّن أول بأول.
>
> ```javascript
> const CHUNK = 15;                       // نزّلها لو كل عنصر بياخد أكتر من نداء
> for (let i = 0; i < items.length; i += CHUNK) {
>   const data = await apiPost('update', { employee, items: items.slice(i, i + CHUNK) });
>   applyResults(data);                                        // الجدول بيتلوّن دلوقتي
>   progressBar.style.width = `${Math.round((i + CHUNK) / items.length * 100)}%`;
> }
> ```
>
> **تلات مكاسب مع بعض:** تقدّم حقيقي · مدة النداء الواحد تحت المهلة ·
> وانقطاع الشبكة بيضيّع **مجموعة** مش دفعة.
>
> 🔴 **والتقسيم بيفتح فخ لازم يتقفل معاه:** أي حارس تكرار في الـ Worker
> (`seenOrders`) **بيشتغل لكل نداء**، فالتقسيم بيعدّيه. لازم تنضيف التكرار
> **في الواجهة كمان** قبل بناء المجموعات. (راجع W-2.)

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟠 H-5 — الترتيب مع pagination server-side لازم يبقى server-side

**التناقض جوّه المهارة نفسها:**
- `data-table-standard.md:432` — `applySort` client-side على `[...rows]`.
- Step 6 (Log Filter Model v2) — **الفلترة والصفحات server-side إلزاميًا**.

النتيجة: الصفحة فيها ١٠٠ صف من أصل ١٢١٧، والترتيب بيرتّب الـ ١٠٠ بس —
**والسهم في الترويسة شكله ترتيب عام**. جدول بيوعد بحاجة وبينفّذ غيرها.

> ℹ️ المهارة **واخدة بالها** من التناقض ده في **العدّ** (`data-table-standard.md:368`
> فيه استثناء موثّق لـ server-side) — بس البند اتكتب للعدّ بس والترتيب فات.

**النص المقترح (بند في `data-table-standard.md` §8):**

> ⚠️ **الترتيب بيتبع الفلترة.** لو الفلترة والصفحات server-side (Log Filter
> Model v2)، **الترتيب لازم يبقى server-side** و`applySort` بتبقى تمرير مباشر:
> ```javascript
> function applySort(rows) { return rows; }   // الترتيب في الـ Worker
> ```
> وأي تغيير في الترتيب **بيرجّع لصفحة ١** — وإلا الصفحة ٣ بترتيب والصفحة ٤ بترتيب تاني.
> `sortBy`/`sortDir` بيتبعتوا لـ `get_logs` **بس** — العدّ مالوش ترتيب،
> والتصدير بياخد ترتيب السيرفر. مصدر باراميترات مختلف لكل نداء = تصدير مش مطابق.

**التصنيف:** 🔴 كاسر (المهارة بتوصف سلوك بيكدب على المستخدم) → **MAJOR**

---

### 🟠 H-6 — `esc()` قبل أي `innerHTML`

**الدليل:** صفر دالة تهريب في المهارة كلها (اتفحصت). وكل قوالبها بتبني
`innerHTML` بـ template literals فيها قيم جاية من APIs خارجية.
في الأداتين كان فيه **٣١ موضع** بيحطّوا نص من بوسطة/شوبيفاي مباشرةً.

**الأثر:** مش ثغرة اختراق (المصادر موثوقة نسبيًا) — لكن `<` واحدة في رسالة خطأ
أو في `businessReference` **بتكسر الجدول أو تبلع باقي الصف من غير أي إشارة**.
"الجدول اختفى نصّه أثناء الشغل" مش أرخص من ثغرة.

**النص المقترح — يتضاف لـ `templates/21_app-shell.html` §CONFIG:**

```javascript
// ─── §CONFIG::esc — إلزامي قبل أي قيمة خارجية تدخل innerHTML ───
function esc(v) {
  return String(v ?? '')
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;').replace(/'/g, '&#39;');
}
```

> **القاعدة:** أي قيمة مصدرها بره الصفحة (API · رسالة خطأ · سجل D1 · إدخال
> مستخدم) بتعدّي على `esc()` قبل `innerHTML` — وده شامل `title="..."`.
> `orderLink()` كمان: الاسم بيتهرّب، والـ `orderId` بيتنضّف لأرقام قبل `href`.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟠 H-7 — مهلة على `apiRequest`

`apiRequest` في المهارة (Step 3) بتفرّق أنواع الفشل كويس — **ومفيهاش مهلة**.
Worker واقف = spinner بيلف للأبد (المتصفح ممكن يستنى دقايق)، والموظف بيفتكر إن
الأداة اتعلّقت **فبيعمل refresh وسط عملية كتابة**.

```javascript
const API_TIMEOUT_MS = 90000;   // طويلة عن قصد: القطع بدري أسوأ من الانتظار

const ctrl  = new AbortController();
const timer = setTimeout(() => ctrl.abort(), API_TIMEOUT_MS);
try {
  resp = await fetch(url, { ...opts, signal: ctrl.signal });
  text = await resp.text();
} catch (e) {
  if (e.name === 'AbortError')
    throw new Error(`الـ Worker ما ردّش خلال ${API_TIMEOUT_MS/1000} ثانية — `
      + `**راجع تاب السجل قبل ما تعيد المحاولة**: ممكن تكون العملية تمّت`);
  throw new Error(`تعذّر الوصول للـ Worker (شبكة أو CORS) — ${e.message}`);
} finally { clearTimeout(timer); }
```

> ⚠️ **نص رسالة المهلة جزء من البند** — "ما ردّش" مش معلومة كافية في أداة
> بتكتب. الموظف لازم يعرف إن العملية **ممكن تكون تمّت**.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟠 H-8 — التصدير مستهلك لـ `res.status` زي أي مستهلك تاني

**الدليل:** أداة الشحن كانت بتصدّر صف `warning` كـ **`—`** — نفس قيمة صف ما
اتحاولش أصلاً. ده **بالظبط** باج "تلات حالات اتحوّلت لاتنين" اللي Step 3C كلها
متكتبة عشانه — قافلينه في الجدول وفي النافذة وفي السجل، **وناجي في التصدير**.
**واللي بيروح للمدير هو التصدير.**

**النص المقترح (بند في Step 3C):**

> ⚠️ **Step 3C بينطبق على التصدير زي الشاشة بالظبط.** أي `res.status` ليه بادج
> في الجدول لازم يكون ليه **نص مقابل في XLSX**، والماب **واحد** بيتقري من
> المكانين. ⛔ ممنوع `success ? 'تم' : error ? 'فشل' : '—'` — الـ `—` دي
> بتاعت "ما اتحاولش"، مش بتاعت "تم جزئيًا".
> ```javascript
> const RESULT_LABELS = { success:'تم', warning:'تم جزئيًا', error:'فشل',
>                         rejected:'مرفوض', already:'خلاص متعمل' };
> ```
> ومعاه **عمود "ملاحظة النتيجة"** — بادج من غير سبب في ملف Excel بلا قيمة.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟡 H-9 — تلات بنود صغيرة في القوالب

| البند | الدليل | الإصلاح |
|---|---|---|
| **`JSON.parse` على `extra` بلا حراسة** في `templates/23_log-tab.html` | صف واحد بـ `extra` تالف كان بيرمي جوّه `.map()` فـ**تاب السجل كله** يبان "❌ فشل التحميل" ومفيش إشارة لأنهي صف — **صف واحد بيوقّع ٦١٥** | `try { … } catch { extra = { _corrupt: true }; }` |
| **اسم ملف التصدير بتوقيت UTC** | `toISOString().slice(0,10)` والباقي بتوقيت القاهرة — تصدير الساعة ١ صباحًا بيتسمّى **بتاريخ إمبارح** | `cairoDateStr(cairoNow())` |
| **اسم الموظف في تاب السجل** | السجل بيخزّن `username` — لازم يتعرض بالاسم المعروض من `get_employees` مع fallback | `employeeDisplayName()` في القالب |

**التصنيف:** 🟡 تحريري → **PATCH**

---

### 🔴 H-10 — Step 9 مابيشوفش دالة متنادية ومش معرّفة (والدرس ده من غلطة أنا عملتها)

**الواقعة:** أثناء تنفيذ إصلاحات النهاردة، **دفعة تعديل عندي فشلت ذريًا وضاعت
منها ٥ تعديلات** — منها **تعريف `esc()`** اللي كانت متنادية في **٣١ موضع**.
النتيجة: `ReferenceError` على أول رسم للنتائج — **الأداة معطّلة بالكامل**.

**وعدّت من كل الفحوص الموجودة:**

| الفحص | النتيجة |
|---|---|
| `node --check` على السكربت | ✅ **عدّى** — الاستدعاء سليم نحويًا |
| Step 9 (grep audit) | ✅ عدّى — مفيش grep بيدوّر على تعريفات |
| Step 9A (`css-check.js`) | ✅ عدّى — ده CSS |

اللي مسكها **مراجعة عكسية مستقلة على الـ diff**، مش أي فحص آلي.

**الدرس (نفس منطق Standards #42 بالحرف):** أي لغة ليها بنية تتفحص **ببنيتها**
مش بـ grep. CSS اتقفل في #42 — **JS لسه مفتوح**: `node --check` بيفحص **النحو**
مش **الربط**، والفرق ده هو بالظبط اللي عدّى.

**النص المقترح — Step 9B:**

```bash
# Step 9B — فحص الربط في JS (إلزامي مع 9 و9A، مش بديل عنهم)
node scripts/js-undef-check.js index.html
# بيستخرج آخر <script> جوّه الصفحة، وبيقارن كل اسم **بيتنادى** بكل اسم
# **معرّف**. `node --check` بيعدّي على دالة متنادية ومش معرّفة — دي بتمسكها.
```

> 🔴 **وقاعدة أوسع اتعلّمناها من نفس الواقعة — وهي أهم من الفحص نفسه:**
> بعد أي جلسة فيها تعديلات متعددة على ملف، **اتحقق من النتيجة المقصودة، مش من
> إن أداة التعديل قالت "تم"**. أداة التعديل ممكن تنجح جزئيًا أو تفشل ذريًا،
> والفرق بين الحالتين **مابيبانش في مخرجاتها**. الفحص العملي: `grep` على
> **كل** رمز جديد اتقال إنه اتضاف (`const X` · `function Y`) وتأكد إنه موجود
> **بتعريفه** مش باستخدامه بس.

الأداة جاهزة ومجرّبة على الأداتين — أقدر أسلّمها كـ `scripts/js-undef-check.js`.

**التصنيف:** 🟠 مُستحسن (بند فحص جديد) → **MINOR**

---

## ٣. `ecommoda-worker-builder`  ·  ٧ بنود  ·  المقترح: **v3.0.0**

### 🔴 W-1 — «خلاص اتعمل» حالة رابعة، ومش فشل

**النص الحالي (Step 5A ④):** `### ④ نتيجة العملية = تلات حالات مش اتنين`

**الدليل الرقمي:**

```
أداة المرتجعات : ٥٩ من ٦١ صف فشل  →  الأوردر كان Returned خلاص
أداة الشحن     : ٦ من ٧ صف رفض    →  الأوردر كان Shipped خلاص
```

يعني **الحالة دي هي أكتر نتيجة متكررة في أدوات السكانر**، وكانت بتتقال بلغة
مهندسين وبلون فشل:

```
"الحالة تغيرت قبل التحديث — [RTO] S1 لازم يكون Shipped أو In-Return (الحالي:
 Returned) — [مرتجع بعد التسليم] S1 لازم يكون Delivered (الحالي: Returned)،
 S2 لازم يكون Shipped أو In-Return (الحالي: —)، returnStatus لازم يكون…"
```

**المعنى الحقيقي جملة واحدة: «ده اتعمل خلاص».**

**الأثر المزدوج:** سجل دايم ١٠٪ منه أحمر كذّاب (فأي تقرير عن نسبة الفشل غلط) ·
والموظف بيتعلّم إن الأحمر طبيعي. **ده نفس المرض اللي Step 5A ③ نفسها اتكتبت
عشانه** ("الحالة الاستثنائية بقت الافتراضية") — بس على الأحمر بدل الأصفر.

**النص المقترح:**

> ### ④ نتيجة العملية = **أربع** حالات مش تلاتة
>
> ```javascript
> const status = alreadyInTargetState ? 'already'
>              : error   ? 'error'
>              : warnings.length ? 'warning' : 'success';
> ```
>
> | الحالة | معناها | اللون | بتتعدّ فين |
> |---|---|---|---|
> | `success` | تم و**اتأكد** | أخضر | ناجح |
> | `warning` | الأساسي تم وفيه حاجة ما اتأكدتش | أصفر | محتاج مراجعة |
> | `error` | حاولنا وفشلنا | أحمر | فشل |
> | **`already`** | **الحالة المستهدفة موجودة أصلاً — مفيش حاجة كانت مطلوبة** | **محايد** | **عدّاد مستقل** |
>
> 🔴 **`already` ممنوع تتعدّ فشل، وممنوع تاخد لون تحذير.** لا حاجة اتحاولت
> وفشلت، ولا كان المفروض تحصل حاجة. ولو اتلوّنت أحمر، **أكتر رسالة بتظهر
> للموظف تبقى أقلها إفادة** — فبيتعلّم يعدّي على الأحمر كله.
>
> **وفحص `already` بيتحط بعد فحوص الصلاحية، مش قبلها** — عشان لو حد وسّع
> قاعدة انتقال بعدين، الصلاحية تغلب بدل ما تتحجب وراه في صمت.
>
> **ورسالته جملة واحدة بلغة الموظف**، مش شرح لكل مسار ما انطبقش:
> `'الأوردر ده اتشحن خلاص قبل كده (S1 = Shipped) — مفيش حاجة مطلوبة'`

**التصنيف:** 🔴 كاسر (بيغيّر عقد `extra.result` وكل عدّاد بيقراه) → **MAJOR**

---

### 🔴 W-2 — تنضيف التكرار: بالكيان اللي بتكتب عليه، مش بالمدخل

**الدليل:** رقمين تتبع بيرجعوا لنفس الأوردر (شحنة + شحنة مرتجع) — حالة **حصلت
فعلاً** في أداة المرتجعات. من غير حارس:

| الأثر | الخطورة |
|---|---|
| **صفّين في D1 لكتابة واحدة** | بيضخّم كل عدّاد وخط أساس |
| `perOrderMeta` بيتكتب فوق نفسه | رقم التتبع الأول بيضيع من السجل |
| `fulfillmentCreate` **مرتين** | التانية بتفشل → **تحذير أصفر كاذب** على أوردر تمام |
| `orderCancel` **مرتين** | **لا رجعة فيه** |

**النص المقترح:**

> #### حارس التكرار — بالكيان مش بالمدخل
>
> لما الدفعة مفهرسة بمدخل (رقم تتبع · SKU) بيتطابق **many-to-one** مع الكيان
> اللي بتكتب عليه (أوردر · متغيّر)، التنضيف بيتعمل **بالكيان**:
>
> ```javascript
> const seenOrders = new Set();
> // ...
> if (seenOrders.has(cleanName)) { rec.error = 'اتنفّذ فعلاً في نفس الدفعة'; continue; }
> seenOrders.add(cleanName);
> ```
>
> 🔴 **والحارس ده لوحده مش كفاية لو الواجهة بتقسّم الدفعة (H-4).**
> `seenOrders` بيشتغل **لكل نداء**، فمجموعتين مختلفتين = حارسين مستقلين
> والتكرار بيعدّي. **التنضيف لازم يتعمل على الطرفين:** في الواجهة قبل بناء
> المجموعات، وفي الـ Worker كحارس أخير.
>
> ⚠️ ولو الواجهة بتطابق النتايج **بالترتيب**، التنضيف لازم يحافظ على مصفوفة
> صفوف موازية للمدخلات — التنضيف من غيرها بيزحلق كل النتايج صف واحد.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🔴 W-3 — `type` بيقسّم حسب «إيه اللي حصل للنظام الخارجي»

**الدليل:** أداة المرتجعات كانت بتسجّل **كل** صف تحت `type='update'` — بما فيهم
٦١ صف فشل **ما لمسوش شوبيفاي أصلاً**. النتيجة إن استعلام خط الأساس الموثّق في
`CLAUDE.md` كان بيقيس **المحاولات مش الكتابة الفعلية** (٦١٥ مقابل ٥٥٢ نجاح).

**وفي أداة الشحن لقيت العكس:** الصف اللي **وصل** لشوبيفاي واترفض كان بيسيب
**صفر أثر** — يعني الحالة **الأهم** (حاولنا وفشلنا) مالهاش سجل، بينما الحالة
الأقل أهمية (اتوقف قبل أي محاولة) بقى ليها.

**النص المقترح:**

> #### `type` بيقسّم بالأثر الخارجي، مش بالـ endpoint
>
> | الحالة | `type` | `extra.result` |
> |---|---|---|
> | الكتابة تمّت | `update` | `success` / `warning` |
> | **اتبعتت للـ API واترفضت** | **`update`** | `error` |
> | اتوقفت **قبل** أي محاولة | `rejected` | `rejected` / `already` |
>
> 🔴 **قاعدة عدم الضياع:** لكل عنصر في الدفعة **صف واحد بالظبط** — يا `update`
> يا `rejected`. صفر صفوف = سؤال "الأداة حاولت ولا لأ؟" مالوش إجابة بعد ما
> الشاشة تتقفل.
>
> 🔴 **ونتيجة مباشرة على التوثيق:** أي استعلام خط أساس أو تقرير **لازم يفلتر
> على `extra.result`**، مش على `type` لوحده:
> ```sql
> … AND type = 'update' AND json_extract(extra,'$.result') = 'success'
> ```
> **الاستعلام اللي بيعدّ `type` بس بيقيس المحاولات** — وده كان مكتوب غلط في
> `CLAUDE.md` بتاع **الأداتين**.

**التصنيف:** 🔴 كاسر → **MAJOR**

---

### 🟠 W-4 — عقد ترتيب `results[]` لازم يتكتب صراحةً

**الدليل:** الأداتين الشقيقتين عندهم عقدين **متعاكسين**، والاتنين **صح**:

| الأداة | العقد | ليه |
|---|---|---|
| المرتجعات | نتيجة لكل عنصر **بترتيب المدخلات** | الواجهة بتطابق **بالفهرس** |
| الشحن | الكتابة الناجحة بتترسم في حلقة تانية → **الترتيب مختلف** | الواجهة بتطابق **بالاسم** |

**مفيش سطر واحد بيوثّق ده في أي مكان.** أول جلسة تنقل منطق بين الأداتين
هتكسر المطابقة **في صمت** — والصفوف تاخد نتايج بعض.

**النص المقترح:**

> #### أي endpoint بيرجّع `results[]` بيعلن عقد الترتيب في تعليق فوقه
> ```javascript
> // العقد: نتيجة واحدة لكل عنصر في `items`، **بنفس الترتيب**، في كل الفروع.
> // ⚠️ الواجهة بتطابق بالفهرس — أي `continue` من غير `push` بيزحلق كل اللي بعده.
> ```
> والواجهة بتكتب في تعليقها **على أنهي عقد بتعتمد**. المطابقة بالاسم آمنة **بس**
> لو التكرار متنضّف (W-2)، والمطابقة بالفهرس آمنة **بس** لو كل فرع بيعمل push.

**التصنيف:** 🟡 تحريري → **PATCH**

---

### 🟠 W-5 — `parseInt` بلا حراسة **في المهارة نفسها**

**النص الحالي (`shared-functions.md:264-265`):**

```javascript
const limit  = Math.min(parseInt(url.searchParams.get('limit')  || '100'), 100);
const offset = Math.max(parseInt(url.searchParams.get('offset') || '0'),    0);
```

`parseInt('abc')` → `NaN` · `Math.min(NaN,100)` → **`NaN`** → بيوصل لـ D1 كـ bind
ويرجّع خطأ غامض.

> 🔴 **البند ده له دلالة خاصة:** أداة المرتجعات أصلحته **محليًا** في v3.2.0
> والمهارة ما اتغيّرتش — **فأداة الشحن اللي اتبنت بعدها ورثت الباج تاني**، ولقيته
> النهاردة. **ده الدليل العملي على إن إصلاح الأداة بدل المهارة بيرجع.**

```javascript
const limitRaw  = parseInt(url.searchParams.get('limit')  || '100', 10);
const offsetRaw = parseInt(url.searchParams.get('offset') || '0',   10);
const limit  = Number.isFinite(limitRaw)  ? Math.min(Math.max(limitRaw, 1), 100) : 100;
const offset = Number.isFinite(offsetRaw) ? Math.max(offsetRaw, 0) : 0;
```

**التصنيف:** 🔴 كاسر (كود المهارة نفسه) → **MAJOR**

---

### 🟠 W-6 — ترتيب server-side بقائمة بيضاء + كاسر تعادل

`shared-functions.md:163,188` بتكتب `ORDER BY timestamp DESC` **حرفيًا** — مفيش
دعم ترتيب خالص، وده اللي دفع الأداتين للترتيب client-side (H-5).

```javascript
// ⚠️ قائمة **مقفولة** — القيمة جاية من العميل وبتتلزق في نص SQL مباشرةً
//    (ORDER BY مابيقبلش bind). أي قيمة بره القايمة بترجع للافتراضي بدون خطأ.
// ⚠️ المفاتيح لازم تطابق `data-sort-key` في الواجهة **حرفيًا** — مفتاح مش في
//    القايمة بيرجع للافتراضي في صمت، فالعمود يبان إنه اترتّب وهو مااترتّبش.
const LOG_SORT_COLUMNS = {
  date: 'timestamp', time: 'timestamp', employee: 'employee', orderName: 'order_name',
  machine: `json_extract(extra, '$.machine')`, result: `json_extract(extra, '$.result')`,
};

function orderByClause(sortBy, sortDir) {
  const col = LOG_SORT_COLUMNS[String(sortBy || '')] || 'timestamp';
  const dir = String(sortDir || '').toLowerCase() === 'asc' ? 'ASC' : 'DESC';
  // 🔴 كاسر تعادل إلزامي: من غيره صفوف نفس القيمة بترتيب عشوائي بين الصفحات،
  //    والصف الواحد ممكن يظهر في صفحتين **أو مايظهرش خالص**.
  return col === 'timestamp' ? ` ORDER BY timestamp ${dir}`
                             : ` ORDER BY ${col} ${dir}, timestamp DESC`;
}
```

> ⚠️ **`getLogsCount` و`getLogsExport` بيتجاهلوا الترتيب** — العدّ مالوش ترتيب،
> والتصدير بياخد ترتيب السيرفر. تمرير الباراميترات ليهم بيفتح باب اختلاف
> الفلاتر بين النداءات.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

### 🟠 W-7 — سقف على أي endpoint بياخد مصفوفة

مفيش أي بند عن ده. المقترح:

```javascript
const MAX_BATCH = 200;   // أكبر دفعة فعلية مقيسة: ٨٢
if (items.length > MAX_BATCH)
  return json({ error: `الدفعة أكبر من الحد (${items.length} من ${MAX_BATCH}) — قسّمها` }, 400, request);
```

> السقف **مش تقييد للاستخدام العادي** — هو حارس ضد لصق قايمة ضخمة بالغلط:
> كل عنصر بياخد نداء خارجي أو أكتر، فدفعة بـ ٥٠٠ بتتحوّل لمئات النداءات
> المتتابعة والمتصفح بيقطع في النص — **وساعتها الكتابة تكون حصلت والواجهة مش
> عارفة**. والرقم يتحدد من **أكبر دفعة مقيسة فعليًا في D1**، مش بالتخمين.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

## ٤. `shopify-graphql-helper`  ·  ٣ بنود  ·  المقترح: **v2.0.0**

### 🔴 S-1 — `orders(query: "name:#X")` **بحث**، مش قراءة بمفتاح

**النص الحالي (`SKILL.md:273`):**
```
✅ name:#40669              — order name (# prefix required)
```
مقدَّم كأنه **قراءة بمفتاح**. وهو **بحث** — وممكن يرجّع أوردر **مقارب**.

**الأثر:** `orders(first: 1, query: "name:#X")` من غير مقارنة الاسم الراجع =
**كتابة على أوردر تاني**. في أداة الشحن ده كان معناه `S1 = Shipped` + فلفلمنت
على أوردر غلط، ويتسجّل في D1 **باسم الأوردر الصح**.

**والنص التاني اللي أخطر:** أداة الشحن كانت بتنضّف الاسم بصمت وتدوّر بالمنضّف
وتحفظ النتيجة تحت **الاسم الأصلي**:

```javascript
const safe = name.replace(/[^a-zA-Z0-9\-]/g, '');   // ❌ استبدال صامت
```

**النص المقترح:**

> #### 🔴 `orders(query: "name:#X")` بحث — لازم تتأكد من اللي رجع
>
> ```javascript
> // ① اسم غير صالح = **يترفض**، مايتنضّفش. التنضيف بيحوّل البحث لاسم تاني
> //    والنتيجة بترجع كأنها بتاعة الأول — استبدال صامت.
> if (name.replace(/[^a-zA-Z0-9\-]/g, '') !== name) { map[name] = null; continue; }
>
> // ② مطابقة الاسم الراجع — أي اختلاف = "غير موجود"، مش كتابة على غيره
> if (!node || cleanOrderName(node.name) !== name) { map[name] = null; return; }
> ```
>
> **القاعدة الأوسع:** لو قيمة محتاجة **تنضيف** عشان تدخل استعلام، والمنضّف
> اختلف عن الأصلي — **ارفض**، متدوّرش على حاجة تانية. والاستعلام اللي بيرجّع
> كيان بمفتاح **لازم يتأكد إن اللي رجع هو اللي طلبته**.

**التصنيف:** 🔴 كاسر → **MAJOR**

---

### 🟠 S-2 — نافذة الـ ٦٠ يوم و`read_all_orders`

**غياب تام من المهارة** (اتفحصت). وهي فخ صلاحيات **بيرجّع نتيجة فاضية مش خطأ**:

> شوبيفاي بتخفي الأوردرات **الأقدم من ٦٠ يوم** عن أي تطبيق مالوش
> `read_all_orders`. البحث بيرجّع **صفر نتيجة** — مش خطأ — فالأداة بتقول
> **"الأوردر غير موجود"** وهي رسالة كاذبة تمامًا.
>
> **أخطر في أدوات المرتجعات:** الشحن بيحصل خلال أيام، لكن **المرتجع ممكن
> يوصل بعد شهرين**.
>
> ✅ ضيف الفحص في `?action=diag` حتى لو الصلاحية مش هتتضاف:
> ```javascript
> add('read_all_orders (أوردرات أقدم من ٦٠ يوم)', scopes.includes('read_all_orders'), …);
> ```

**التصنيف:** 🟠 مُستحسن → **MINOR**

### 🟡 S-3 — بندين أصغر

| البند | التفصيل |
|---|---|
| **`first: N` بلا pagination لازم يكشف القصّ** | `pageInfo { hasNextPage }` ببلاش. استرجاع جزئي بيتقال عنه "تم" = فشل صامت |
| **`OrderCancelReason` مش وصف حر** | `DECLINED` معناها **"الدفع اترفض"** مش "العميل رفض الاستلام". لـ RTO الأقرب `CUSTOMER`. ⚠️ أنا نفسي اقترحت `DECLINED` في التقرير الأصلي **وسحبت الاقتراح** بعد المراجعة — كان هيبوّظ التقارير |

**التصنيف:** 🟡 تحريري → **PATCH**

---

## ٥. `ecommoda-constants`  ·  بندين  ·  المقترح: **v1.11.0**

### 🔴 C-1 — `type = 'rejected'` (بند مفتوح **اتوسّع**)

القيمة بتتكتب من `bosta-orders-shipped-scanner` من v3.3.0، **ومن
`bosta-orders-returned-scanner` كمان** من v3.4.0 — **كاتبين مش واحد**.

```
| Metafields Change Log | metafields_change | update · rejected · login · logout |
```

> **والدرس أوسع من الصف نفسه:** القاعدة "التسجيل قبل أول `writeLog`" اتخرقت
> **مرتين بنفس القيمة**. المهارة أصلاً بتقول إن ادعاء التسجيل في `CLAUDE.md`
> **مش دليل** (حالة `order_item_remover`) — الاقتراح إن البند ده يترفّع لقائمة
> تسليم صريحة: **أي `type` جديد لازم يكون في §7 قبل أول merge**، والتحقق بـ
> `grep` على المهارة مش بقراءة `CLAUDE.md`.

### 🟠 C-2 — قسم جديد: مفردات `extra.result`

`type` مسجّلة، لكن `extra.result` بقت **قرار تشغيلي** — أي تقرير بيعدّ الرفض
لازم يعرف إن `already` مش رفض:

```
success · warning · error · rejected · already
```
> ⚠️ **أي تقرير بيعدّ "الرفض" لازم يستثني `already`** — وإلا بيعدّ "مفيش حاجة
> مطلوبة" كرفض حقيقي. و**`extra.stage`** (`lookup` / `write`) بيفرّق بين
> "اترفض من الاستعلام" و"اتوقف وقت الكتابة" — التانية سباق بين موظفين.

**التصنيف:** 🟠 مُستحسن → **MINOR**

---

## ٦. `ecommoda-debugger`  ·  بندين  ·  المقترح: **v?.?+1**

### 🟠 D-1 — عيلة فشل جديدة: **«الأداة بتقول فشل على عملية نجحت»**

> **العرض:** نسبة فشل عالية في السجل، والموظف بيقول "الأداة شغّالة عادي".
> **السبب:** الشاشة بعد النجاح شكلها زي قبل الضغط، فالدفعة بتتبعت مرتين
> والتانية بترجع "الحالة اتغيّرت".
> **التشخيص — استعلام واحد:**
> ```sql
> SELECT COUNT(*) FROM logs e WHERE json_extract(e.extra,'$.result')='error' AND EXISTS (SELECT 1 FROM logs s WHERE json_extract(s.extra,'$.result')='success' AND s.order_name=e.order_name AND s.timestamp<e.timestamp);
> ```
> **لو الرقم قريب من إجمالي الفشل → الفشل كذّاب.** (القياس الحقيقي: ٥٩ من ٦١.)
> **العلاج:** H-2 (نزع السلاح) + W-1 (`already`).

### 🟠 D-2 — «الصفحة بتفتح والكونسول نضيف والأداة مش شغّالة»

> دالة متنادية ومش معرّفة. `node --check` **بيعدّي** عليها (النحو سليم).
> **الفحص:** Step 9B في `ecommoda-html-builder`.
> **والقاعدة الأوسع:** بعد أي جلسة تعديلات متعددة، **اتحقق من النتيجة
> المقصودة مش من إن أداة التعديل قالت "تم"** — الفشل الجزئي مابيبانش في
> مخرجاتها.

---

## ٧. حاجات صلّحتها و**قررت إنها مش تستاهل تعديل مهارة**

الأمانة هنا مهمة زي التوصيات:

| الإصلاح | ليه مش مهارة |
|---|---|
| `Metafield.owner` بدل `ownerId` | **موثّق أصلاً** في `shopify-graphql-helper` ومطبّق في الأداتين |
| `cleanOrderName` وفخّ الـ `#` | **موثّق أصلاً** في `bosta-api-helper` و`ecommoda-constants` §3.3 |
| ترجمة رسايل شوبيفاي للعربي | حالات خاصة بأداة المرتجعات — القاعدة العامة ("مترجم مع الأصل") **موجودة ضمنيًا** في Step 5A |
| تنبيه صفوف ما قبل تسجيل النتيجة | خاص بتاريخ الأداتين، مش نمط متكرر |
| تنضيف الحالة عند تسجيل الخروج | **موجود** كـ `// CUSTOMIZE` في Step 4 — الأداة سابته فاضي. مخالفة تنفيذ مش نقص قاعدة |
| حارس `isConfigured()` في `checkWorkerVersion` | **Standards #29 موجود، وStep 9 بيتفحصه بـ grep** — يعني الفحص كان هيمسكه لو اتشغّل. 👈 **بس منه بند حقيقي واحد** ↓ |

### 🟡 البند الوحيد اللي طلع من القايمة دي

> **Step 9 يتشغّل على الأداة قبل ما تبدأ التعديل، مش قبل التسليم بس.**
> أداة المرتجعات كانت مخالفة Standards #29 **قبل** ما أفتحها، والفحص الآلي
> اللي بيمسكه موجود من زمان — بس بيتشغّل **بعد** التعديل. جردة أساس في أول
> الجلسة بتكشف المخالفات القديمة بدل ما تعيش لحد ما حد يلاحظها بالصدفة.
>
> (وده مؤيَّد بسجل المهارة نفسها: باج الـ z-index اتكرر **مرتين** بعد توثيقه.)

---

## ٨. ترتيب التنفيذ المقترح

| # | البند | المهارة | ليه الترتيب ده |
|---|---|---|---|
| **١** | **H-1** نمط السكانر | html-builder | **كل أداة فيها سكانر مصابة دلوقتي.** أرخص إصلاح وأوسع أثر |
| **٢** | **W-5** `parseInt` | worker-builder | كود المهارة غلط، والدليل إنه رجع فعلاً |
| **٣** | **H-2** نزع السلاح | html-builder | أكبر أثر مقيس (٩٧٪) — بس محتاج شغل في كل أداة كتابة |
| **٤** | **W-1** حالة `already` | worker-builder | بيمشي مع H-2؛ 🔴 كاسر لعقد `extra.result` |
| **٥** | **S-1** مطابقة الاسم | graphql-helper | كتابة على كيان غلط — أخطر نوع، بس أضيق انتشارًا |
| **٦** | **H-3** التأكيد · **W-3** `type` | الاتنين | بيمسّوا أدوات الأفعال اللي مالهاش رجعة |
| **٧** | **H-10** Step 9B · **D-1/D-2** | html-builder · debugger | فحوص بتمنع تكرار الباقي |
| **٨** | الباقي (🟡) | متفرّقة | مع أول تعديل على كل مهارة |

> ⚠️ **البنود 🔴 الكاسرة (W-1 · W-3 · S-1 · H-5 · W-5 · H-1) لازم يتكتب معاها
> سطر `🔴 معلّقة` في `CLAUDE.md` بتاع كل أداة بصمتها أقدم** — وإلا التغيير
> بيبقى **غير مرئي** والأداة تفضل شغّالة بالقاعدة القديمة من غير أي خطأ
> (`ecommoda-constants` §10).

---

## ٩. ملحق — جرد الأدوات المتأثرة

الأداتين اتصلّحوا. **الباقي لسه مجهول** — الأوامر دي بتجاوب:

```bash
# ① نمط السكانر المكسور (H-1) — أي نتيجة = الأداة مصابة
grep -rn "gap < 80" --include="*.html" .

# ② parseInt بلا حراسة في get_logs (W-5)
grep -rn "Math.min(parseInt" --include="*.js" .

# ③ ترتيب client-side مع pagination server-side (H-5)
grep -rln "applySort" --include="*.html" . | xargs grep -ln "get_logs_count"

# ④ بحث بالاسم من غير مطابقة (S-1) — راجع كل نتيجة بعينك
grep -rn 'query: "name:#' --include="*.js" .

# ⑤ استبدال صامت للاسم قبل البحث (S-1)
grep -rn "replace(/\[^a-zA-Z0-9" --include="*.js" .

# ⑥ نص خارجي في innerHTML من غير تهريب (H-6)
grep -rLn "function esc(" --include="*.html" . | xargs grep -ln "innerHTML"

# ⑦ JSON.parse على extra بلا حراسة (H-9)
grep -rn "JSON.parse(e.extra\|JSON.parse(entry.extra" --include="*.html" .
```

> ⚠️ **الأوامر دي بتتشغّل على ريبوهات مش في نطاق الجلسة دي** — النتايج محتاجة
> مراجعة بعينك قبل أي تعديل جماعي.

---

## ١٠. الخلاصة

**١٩ بند** على ٥ مهارات. أهم حاجة فيهم مش عدد البنود — **دي إن تلاتة منهم
أخطاء مكتوبة جوّه المهارات نفسها**، يعني كل أداة اتبنت صح **ورثتهم**:

- نمط السكانر بفرع ميّت → كل أداة فيها سكانر
- `parseInt` بلا حراسة → كل أداة فيها تاب سجل
- `ORDER BY` ثابت → كل أداة فيها ترتيب على جدول مصفّح

**والبند اللي أنا أكثرهم اقتناعًا بيه (W-5)** لأن له دليل قاطع: أداة المرتجعات
أصلحته محليًا من ٥ أيام، والمهارة ما اتغيّرتش، **فأداة الشحن ورثته تاني ولقيته
النهاردة**. أي إصلاح بيتعمل في أداة من غير ما يرجع للمهارة **بيرجع**.

**والدرس الأعمق من كل ده — والمشترك بين نص البنود:** الأداتين كانوا ممتازين في
"مفيش نجاح من غير دليل" على **طبقة الاتصال الخارجي**، وضعاف في نفس المبدأ على
**الطبقة اللي الموظف بيلمسها**. المهارات بتغطّي الأولى بعمق (Step 5A كامل)
وبالكاد بتلمس التانية. **البنود H-2 · H-3 · H-4 · W-1 كلها نفس الفجوة دي.**

---

آخر تحديث: 08-09-2026 — 10:15

</div>
