<div dir="rtl" style="text-align: right;">

# سكانر شحن الأوردرات من بوسطة (`Bosta-Orders-Shipped-Scanner`)

![version](https://img.shields.io/badge/version-v2.4.0-blue)

**بتعمل إيه:** الموظف بيسكان تراكينج نمرة بوسطة، الأداة بتتأكد من نوع الشحنة وحالتها الحالية على شوبيفاي (S1/S2)، ولو الانتقال صحيح بتكتب الحالة `Shipped` وتعمل Fulfillment تلقائي.
**مين بيستخدمها:** المخزن — نقطة الشحن.
**الإصدار:** Worker `v3.4.0` · الواجهة `v3.5`

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Bosta-Orders-Shipped-Scanner/
الـ Worker : https://bosta-orders-shipped-scanner.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: bosta-orders-shipped-scanner     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `check_employee` GET | فحص وجود الموظف وحالة PIN |
| `register_pin` POST | تسجيل PIN لأول مرة |
| `verify_employee` POST | تسجيل الدخول بالـ PIN |
| `log_logout` GET | تسجيل خروج |
| `get_employees` GET | قائمة الموظفين النشطين |
| `lookup` POST | بحث بوسطة (`trackingNumbers[]`) + فحص S1/S2 على شوبيفاي + التحقق من صلاحية الانتقال — بدون كتابة |
| `update` POST | إعادة تحقق من الحالة وقت الكتابة + `metafieldsSet` (S1 أو S2 = Shipped) + `fulfillmentCreate` (لو فيه OPEN fulfillmentOrders) + D1 log · وبياخد كمان `rejected[]` فبيسجّلها بـ `type = 'rejected'` **من غير أي كتابة** |

> من واجهة v3.3: الضغط على **"تحديث شوبيفاي"** بينادي `lookup` تاني **قبل** `update`
> — لو أي أوردر مختار اتغيّرت حالته أو ما اتأكدتش، الدفعة بتتوقف ومفيش `update`
> أصلاً. فحص الـ Worker وقت الكتابة باقي زي ما هو — ده حارس تاني مش بديل.
| `get_logs` | سجل العمليات — فلاتر server-side كقوايم (`employees` · `types` · `results` · `machines`) + `search` + `dateFrom`/`dateTo` + pagination + **ترتيب server-side** (`sortBy`/`sortDir` بقائمة بيضاء مقفولة) |
| `get_logs_count` | عدد الصفوف المطابقة **لنفس الفلاتر بالظبط** |
| `get_logs_export` | تصدير كامل حتى `LOG_EXPORT_MAX` (2000) — وبيرجّع `cap` و`total` و`truncated` معاه |
| `diag` GET | فحص ذاتي بدون كتابة: المتغيرات (اسم وطول فقط) · صلاحيات تطبيق شوبيفاي · بوسطة · D1 · الـ Origin |
| `get_config` GET | `WORKER_VERSION` — الواجهة بتقارنه بـ `MIN_WORKER_VERSION` عندها |

قاعدة الانتقال الوحيدة اللي الأداة دي بتفرضها (مؤكدة من أحمد):
```
Bosta orderType = "Send"     → لازم S1 = Ready               → يكتب S1 = Shipped
Bosta orderType = غير كده     → لازم S1 = Delivered و S2 = Ready → يكتب S2 = Shipped
```
أي حالة تانية بترفض مع سبب واضح ومتكتبش صامتة (order-lifecycle Rule 10).

> ⚠️ **من v3.4.0 فيه حالة رابعة: `already`.** لو الحالة الحالية هي **الحالة
> المستهدفة نفسها** (Send و S1 = Shipped · غير Send و S2 = Shipped)، ده **مش
> رفض** — ده "مفيش حاجة مطلوبة". بياخد بادج محايد وعدّاد مستقل و`extra.result
> = 'already'`. السبب رقمي: ٦ من ٧ صفوف الرفض المسجّلة كانت الحالة دي بالظبط،
> وكانت بتتقال بلغة تقنية ("S1 ليس Ready (الحالي: Shipped)") وبلون رفض —
> فأكتر رسالة بتظهر للموظف كانت أقلها إفادة، وبيتعلّم يعدّي على الرفض كله.

## D1

```
tool  : bosta_tracker         → type: login · logout   (فقط — بقايا نظام التاجات القديم)
tool  : metafields_change     → type: update            (كتابة الحالة الفعلية)
                              → type: rejected          (v3.3.0 — أوردر اترفض، مفيش أي كتابة)
                                 extra.sourceTool = "bosta_orders_shipped_scanner"
                                 extra.result = 'rejected' | 'already'   (v3.4.0)
                                 extra.stage  = 'lookup'   | 'write'     (v3.4.0)
```

> **`extra.stage` بيفرّق حالتين مختلفتين تمامًا** تحت نفس النوع:
> `lookup` = اترفض وقت الاستعلام (الموظف شافه أحمر قبل ما يضغط) ·
> `write` = **كان متوافق واتوقف وقت الكتابة** — يعني سباق: موظف تاني حرّك
> الأوردر في الوقت الفاصل. التانية دي كانت **مالهاش أي أثر في D1 قبل v3.4.0**،
> رغم إنها أكتر حالة محتاجة أثر.

> `metafields_change` سجل مشترك بين أكتر من أداة — الفصل بمفتاح `extra.sourceTool`
> (راجع `ecommoda-constants` §7).

> 🔴 **`type = 'rejected'` قيمة جديدة لسه ما اتسجلتش في `ecommoda-constants` §7.**
> القاعدة هناك: أي قيمة تتسجّل **قبل** أول `writeLog`. الأداة دي بقت بتكتبها من
> v3.3.0، فالتسجيل في الجدول (صف "Metafields Change Log" → `update` · `rejected`)
> **مطلوب من أحمد** وإلا تبقى نفس مخالفة `shopify_woo_sync` (صفوف اتكتبت قبل
> التسجيل). ولاحظ إن الأداة دي بقت **رابع كاتب** تحت `metafields_change`.

> ⚠️ **الرفض في `type` مستقل — مش في `update` بعلم.** خط الأساس تحت بيعدّ
> `type = 'update'` بس، فلو الرفض اتكتب تحت نفس النوع كان عدّاد الكتابة الفعلية
> هيتضخّم ومقارنة ما بعد النقل تبقى بلا معنى.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET · BOSTA_API_KEY
Vars     : SHOP_DOMAIN                                   ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي — لسه ما اتضيّقش)
```

## CORS

**Option B — قائمة صارمة** (اتحوّلت من `wildcard *` في v3.2.1، قرار أحمد 03-09-2026):

```javascript
const ALLOWED_ORIGINS = ['https://ecommoda-dev.github.io'];
```

السبب: الأداة بتكتب على شوبيفاي (ميتافيلد + فلفلمنت)، فبتاخد نفس نمط باقي أدوات
الكتابة. أي أصل غير مسموح بياخد ردًا فيه `Access-Control-Allow-Origin` بتاع
الأصل المسموح — مش أصله هو — فالمتصفح بيرفض القراءة.

⚠️ **الافتراضي في `json()` بقى أول أصل مسموح، مش `*`.** نداء `json()` ناسي
الـ `request` كان هيرجّع رد بـ `*` وسط ردود مقيّدة — يعني رسائل الخطأ (أكتر حاجة
بتتسرّب) تفضل مقروءة من أي أصل. الثغرة دي بتتفتح بالنسيان مش بقرار، فالافتراضي
نفسه اتقفل. **متفتحوش تاني.**

⚠️ **نتيجة عملية:** فتح `index.html` من `file://` أو من أي دومين تاني مش هيشتغل
مع الـ Worker — ده مقصود. الاختبار المحلي يتعمل على نسخة GitHub Pages.

## خط الأساس بعد النقل — ✅ **اتقفل 07-09-2026**

> ما اتاخدش رقم مباشر من أحمد قبل النقل. البديل من D1 (26-08 §0-ب): عدد صفوف
> الكتابة الفعلية (`metafields_change`/`update`, `extra.sourceTool = bosta_orders_shipped_scanner`)
> **قبل النقل = 893 صف**، آخر واحد `2026-09-02T11:35:43Z`. المقارنة بعد النقل
> على نفس الاستعلام:

```sql
SELECT COUNT(*) as total, MAX(timestamp) as last_ts FROM logs WHERE tool = 'metafields_change' AND type = 'update' AND extra LIKE '%"sourceTool":"bosta_orders_shipped_scanner"%';
```

> ✅ **القياس الفعلي 07-09-2026: 1217 صف** (+324 من النقل)، آخر واحد
> `2026-09-07T13:14:58Z`. الأداة بتكتب من الريبو فعلاً والعدّاد بيزيد —
> **بند ١٠ مقفول**. وكمان: ٣٢٤ صف كلهم `result = success`، **صفر تحذير وصفر
> فشل**، و٣٢٤/٣٢٤ فلفلمنت اتعمل بنجاح — يعني مسار الفلفلمنت (v3.1.0) شغّال
> ومقيس مش مفترض.

> ⚠️ **الاستعلام ده بيعدّ `type = 'update'` بس، وده مقصود ولازم يفضل كده.**
> الرفض و"خلاص اتعمل" بيتكتبوا تحت `type = 'rejected'` — لو اتحطّوا جوّه
> `update` كان العدّاد هيقيس **المحاولات** مش الكتابة الفعلية.

## فخاخ الأداة دي

- `businessReference` بتيجي من بوسطة بالـ `#` — أي مطابقة لازم تعدّي على
  `cleanOrderName()` وإلا كل أوردر بيترفض بـ "غير موجود على شوبيفاي" (باج
  v2.0.0 المُصلَّح في v3.0.1).
- `Metafield` مفيهوش حقل `ownerId` — الحقل الصح `owner { ... on Order { id } }`
  (مُصلَّح في v3.0.3).
- الفلفلمنت بيتعمل **بعد** كتابة الميتافيلد بنجاح، ومفيش rollback لو فشل —
  الصف بياخد `status = "warning"` (مش `success`) وبيتسجل في `extra.fulfillment`
  و`extra.result`.
- `.col-num` **ماكانش معرّف في الأداة دي** — الموجود `.col-mono`. الكلاس المش
  معرّف مابيغلطش، بيسكت: الخلايا بترجع `direction: rtl` فالتواريخ والأرقام
  بتتقلب. الاتنين معرّفين دلوقتي بنفس القيمة (v3.2).
- `checkWorkerVersion()` بتنادي `apiGet` اللي بتفتح شاشة الإعدادات لو مش
  مضبوطة — فيه حارس `isConfigured()` قبلها، وإلا الإعدادات بتفتح لوحدها فوق
  شاشة الدخول أول ما الموظف يفتح الأداة (v3.2).
- `dateFrom`/`dateTo` في فلتر السجل بيتقارنوا بالـ `timestamp` المخزّن (**UTC**)
  والعرض بتوقيت القاهرة (UTC+3) — فرق التلات ساعات ممكن يحط عملية بعد ٩ مساءً
  في يوم UTC اللي بعده. مقبول لفلتر بالأيام، ومكتوب هنا عشان مايتكتشفش كباج.
- **إعادة الاستعلام لازم تستبدل الصف، مش تتخطّاه.** لحد v3.2 كان
  `startLookup()` بيتخطّى أي رقم موجود في `resultsData`، فالضغط على الزرار
  تاني كان بيعرض حالة اتقرأت من دقايق والزرار يبان إنه استعلم. متصلَّح في
  v3.3 (`refreshRow`/`mergeLookupResults`) — أي تعديل جاي على الدمج يفضل
  محافظ على القاعدة: الصف المعروض = آخر استعلام فعلي.
- نتيجة كتابة سابقة (`shopifyStatus`/`actions`/`warning`) **بتتحافظ عليها** عبر
  إعادة الاستعلام — دي سجل لفعل حصل، مش حالة بتتقرا من شوبيفاي. اللي بيتحدّث
  هو `s1`/`s2`/`valid`/حالة بوسطة بس.
- تغيّر **حالة بوسطة** لوحده مش سبب لإيقاف الكتابة في `recheckBeforeUpdate` —
  الصلاحية متوقفة على نوع بوسطة + S1/S2 بس، والإيقاف على حاجة مالهاش أثر على
  القرار بيولّد تحذير كاذب متكرر الموظف بيتعلّم يتجاهله.
- **الرفض بيتسجّل مرة واحدة لكل سبب.** الواجهة بتعلّم الصف بـ
  `rejectLoggedReason` بعد نجاح التسجيل، و`pendingRejectedRows()` بتستثنيه.
  من غير ده كل ضغطة على "تحديث شوبيفاي" على نفس الجدول كانت هتكرّر نفس صف
  الرفض في سجل دايم. العلامة بتتحافظ عليها في `refreshRow` (زيّ نتيجة الكتابة)
  ومربوطة **بالسبب**: السبب لو اتغيّر، الصف بيترجّع للتسجيل — دي واقعة جديدة.
- **سبب الرفض المسجَّل بيتحسب في الـ Worker وقت التسجيل، مش من اللي الواجهة
  بعتته.** اللي جاي من الواجهة سياق (بوسطة) بس. والأوردر اللي بقى **متوافق**
  بين الاستعلام والتسجيل مابيتسجّلش مرفوض خالص — بيرجع بـ `becameValid` والصف
  بيتحدّث في الجدول. صف رفض كذّاب في سجل دايم أسوأ من صف ناقص.
- **دفعة كلها مرفوضة زرارها بيتغيّر اسمه** لـ "⛔ تسجيل المرفوض" — بيسجّل بس
  ومابيكتبش. زرار اسمه "تحديث شوبيفاي" وهو مش هيحدّث حاجة = نجاح كاذب في اسم.
- صفوف السجل اللي اتكتبت **قبل v3.2** مالهاش `extra.result`، وعمود "النتيجة"
  بيعرضها **"—" مش "✓"** — إحنا فعليًا مش عارفين إن كان الفعل تم بالكامل وقتها.
  (٨٩٣ صف = ٧٣٪ من السجل. من v3.5 فيه تنبيه فوق الجدول بيظهر لما الصفحة
  المعروضة فيها صفوف من دول.)
- **الصف اللي اتنفّذ بيتنزع سلاحه (`doneInSession`) — ومايترجعش للاختيار.**
  قبل v3.5 كانت الصفوف بتفضل مختارة بعد الكتابة والزرار بيفضل بنفس العدد،
  يعني الشاشة بعد النجاح شكلها زي قبل الضغط. العلامة دي **بتتحافظ عليها في
  `refreshRow`** (زيّ نتيجة الكتابة) — إعادة الاستعلام مابتنزعهاش. والاستثناء
  الوحيد: صف **فشل** بيفضل قابل لإعادة المحاولة (ممكن يكون خطأ شبكة عابر)،
  والحماية عليه هي إعادة الاستعلام الإجبارية قبل الكتابة.
- **الـ Worker بيرجّع `results` مش مرتبة زي `items`.** الكتابة الناجحة بتترسم
  في حلقة تانية على `toWrite`، فالواجهة بتطابق **باسم الأوردر** مش بالترتيب.
  ده آمن بس **بعد** حارس `seenOrders` (v3.4.0) اللي بيضمن اسم واحد لكل دفعة.
  ⚠️ أداة المرتجعات عكسها بالظبط (بتطابق بالترتيب) — متنقلش المنطق بينهم.
- **الاسم غير الصالح بيترفض، مابيتنضّفش.** قبل v3.4.0 كان `businessReference`
  اللي فيه أي حرف غير `[a-zA-Z0-9-]` بيتحوّل لاسم تاني والبحث يتم بيه
  **والنتيجة تتحفظ تحت الاسم الأصلي** — يعني كتابة Shipped وفلفلمنت على أوردر
  غلط. ومعاه دلوقتي مطابقة الاسم الراجع من البحث. **الحارسين دول مايتشالوش**:
  البيانات الحالية كلها أرقام صافية، وده حظ في البيانات مش أمان في الكود.
- **رقمين تتبع لنفس الأوردر في دفعة واحدة** (شحن + إعادة شحن) كانوا بيدّوا:
  مدخلين متطابقين في نفس الـ chunk · `perOrderMeta` بيتكتب فوق نفسه (رقم
  التتبع الأول بيضيع) · **صفّين في D1 لكتابة واحدة** (بيضخّم خط الأساس تحت) ·
  و**`fulfillmentCreate` مرتين** فالتانية بتفشل وتطلّع تحذير أصفر كاذب.
  الحارس `seenOrders` (v3.4.0) بيقفلهم كلهم. ⚠️ ما ظهرش في الإنتاج لحد
  07-09-2026 (صفر تكرار في نفس الدفعة) — الحارس وقائي، والحالة حصلت فعلاً في
  أداة المرتجعات وعشان كده الحارس اتضاف هناك الأول.
- **التحديث بيتقسّم على مجموعات ١٥ في الواجهة.** مش تحسين شكلي: نداء واحد
  لـ ٨٢ أوردر ≈ ٩٢ نداء شوبيفاي متتابع ≈ ٢٠-٣٥ ثانية والشاشة ساكتة تمامًا،
  والسكوت ده هو الوقود المباشر للضغطة التانية. **متحوّلهاش لنداء واحد تاني.**

## ⚠️ §BOSTA اتنسخ في `Orders-Packing-Checker` (05-09-2026)

أداة **Pack Checker** (`Orders-Packing-Checker`) ضافت في v2.5.0 مدخل «ماسح
باركود بوسطة»: بتحوّل تراكينج بوسطة لرقم أوردر على شوبيفاي عشان تفتح شاشة
التغليف. الكود اللي بتستخدمه **منقول حرفيًا من الأداة دي**:

```
Bosta-Orders-Shipped-Scanner/index.js  §BOSTA::bostaSearch · extractDeliveries · §HELPERS::cleanOrderName
Orders-Packing-Checker/index.js        §BOSTA::bostaSearch · extractBostaDeliveries · cleanOrderName
```

**يعني المنطق ده دلوقتي في مكانين** — نفس عيلة خطر `classifyS2Subtype` بين
`Pack Checker` و`Ready-to-Pack`، واللي أنتج فعلاً باج فضل شهور. أي إصلاح على:

- الأشكال الأربعة لرد بوسطة (`extractDeliveries`)
- `cleanOrderName` وفخّ الـ `#` في `businessReference`
- قاعدة «الفشل بيرمي مايرجّعش قايمة فاضية» (مفتاح غلط ≠ شحنة غير موجودة)
- ترويسة `Authorization` من غير `Bearer`

**لازم يتعمل في الريبوهين.**

الفروق المقصودة (مش انحراف يتصلّح):

| | الأداة دي | Pack Checker |
|---|---|---|
| بتكتب على بوسطة؟ | ❌ (قراءة بس) | ❌ (قراءة بس) |
| `STATE_MAP` بيدخل في قرار؟ | ✅ لأ — لكن الحالة بتتعرض وبتتسجّل | ❌ عرض بس، صفر قرار |
| القرار متوقّف على إيه؟ | نوع بوسطة + S1/S2 (`validateTransition`) | شوبيفاي وحدها (`analyzeStage`) |
| بتشتغل على دفعة؟ | ✅ `trackingNumbers[]` | ❌ رقم واحد لكل سكان |

> الأداة دي **ما اتغيّرش فيها حاجة** في التعديل ده — السطور دي توثيق للخطر بس.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة القديمة (1.1.html · 3.0.html · 3.0.1.html · 3.0.3.html · 3.1.html) محفوظة في commit: 3a2c551
git show 3a2c551^:1.1.html
```

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.1.0 |
| ecommoda-html-builder | v6.6.0 |
| ecommoda-constants | v1.10.0 |
| ecommoda-order-lifecycle | v1.2.0 |
| shopify-graphql-helper | v1.0.0 |
| bosta-api-helper | — (خارج نظام الإصدارات — مفيش سطر إصدار في المهارة) |

آخر مطابقة: 07-09-2026 · `index.js` v3.4.0 · `index.html` v3.5
🔴 معلّقة: — لا شيء

## مسائل مفتوحة

- ⚠️ **§BOSTA بقى في ريبوهين** (راجع القسم فوق) — أي إصلاح لازم يتعمل في
  `Orders-Packing-Checker` كمان لحد ما الاتنين يشاركوا مصدر واحد.
- 🔴 **تسجيل `type = 'rejected'` في `ecommoda-constants` §7 — مطلوب من أحمد،
  والبند ده اتوسّع في 07-09-2026.** القيمة بتتكتب من v3.3.0 هنا، و**بقت
  بتتكتب من `bosta-orders-returned-scanner` كمان** من v3.4.0 — يعني كاتبين
  مش واحد. الصف المطلوب في الجدول (صف "Metafields Change Log"):
  `update` · `rejected` · `login` · `logout`. من غير التسجيل ده، ده تكرار
  لنفس مخالفة `shopify_woo_sync` (صفوف اتكتبت قبل التسجيل) — ومرتين بدل مرة.
- 🟡 **`extra.result = 'already'` و`extra.stage`** — قيم جديدة جوّه `extra`
  (مش قيم `type`)، فمش داخلة في جدول §7 تقنيًا. بس أي تقرير مستقبلي بيعدّ
  "الرفض" لازم يستثني `result = 'already'` وإلا بيعدّ "مفيش حاجة مطلوبة"
  كرفض حقيقي.
- 🟡 **`read_all_orders` مش من صلاحيات التطبيق.** فحص `diag` بقى بيقولها من
  v3.4.0. غيابها **مش عطل حالي** (كل الأوردرات المستخدمة أحدث من ٣٠ يوم)، لكن
  شوبيفاي بتخفي أي أوردر أقدم من **٦٠ يوم** عن تطبيق مالوش الصلاحية — فأول
  شحنة بطيئة هتطلّع "الأوردر غير موجود على شوبيفاي" وهي رسالة كاذبة تمامًا.
  القرار (تتضاف ولا لأ) لأحمد.
- 🟡 **PIN بيتخزّن plaintext ومفيش حد لمحاولات الدخول** (٤ أرقام = ١٠ آلاف
  احتمال). الكود جوّه بلوك `§SHARED` المكتوب فوقه "انسخه حرفيًا" — **الإصلاح
  مكانه `ecommoda-worker-builder` مش الأداة دي**، وأي تعديل محلي هيتمسح مع
  أول تحديث للبلوك. البند مسجّل هنا عشان مايتنسيش.
- 🟡 **`WORKER_SECRET` عند العميل = نسبة العمليات للموظف مش دليل.** أي موظف
  يقدر ينادي `?action=update` مباشرةً ويكتب أي `employee` في السجل. قيد
  معماري على مستوى الستاك (توكن لكل موظف)، مش على الأداة دي لوحدها.
- Build watch paths لسه `*` الافتراضي — تضييقها لـ `index.js` + `wrangler.toml` قرار داشبورد لأحمد (§13-ب في السكيل).
  ⚠️ **الفخ المصاحب** (اتعلّمناه من أداة المرتجعات): لو الأداة كبرت وضافت ملف
  جديد بيعتمد عليه الـ Worker، لازم يتضاف للـ paths وإلا الـ Worker يفضل على
  نسخة قديمة **من غير أي رسالة**.
- `ADMIN_WORKER_URL` في `§CONFIG` اتكتب من `ecommoda-constants` §5b — لو القيمة
  دي مختلفة عندك، عدّلها هناك في الكود (مش في شاشة الإعدادات، الحقل اتشال).

### ✅ محسوم — "سبب تغيير الحالة" على S1 لا ينطبق على الأداة دي

`ecommoda-order-lifecycle` §1.5 بيقول إن **أي Worker بيكتب `custom.manual_status`
لازم يطلب سبب** (إلزامي في الواجهة + رفض `400`). الأداة دي بتكتب `S1 = Shipped`،
فظاهريًا داخلة في النطاق.

**قرار أحمد 03-09-2026: الأداة دي مستثناة.** نص البند نفسه بيقول
«Today only **Order Status Updater** writes S1 **manually**» — والقاعدة معناها
"لما موظف **يختار** حالة، سجّل ليه". هنا الموظف مابيختارش حالة أصلاً: الأداة
بتشتق الانتقال الوحيد المسموح من نوع الشحنة في بوسطة + الحالة الحالية على
شوبيفاي، والسبب هو **حدث السكان نفسه**. وحقل سبب إلزامي على كل سكان هيبطّأ نقطة
الشحن من غير معلومة جديدة.

⚠️ **ممنوع أي جلسة جاية "تصلّحها"** بإضافة حقل سبب هنا — ده مش بند ناقص، ده
استثناء متقرر. لو الاستثناء ده اتكتب في `§1.5` نفسها بعدين، السطر ده يتشال.

## بعد نشر v3.4.0 / الواجهة v3.5 — تأكيدات مطلوبة

> ⚠️ **الواجهة بتطلب Worker ≥ v3.4.0** (`MIN_WORKER_VERSION`). لو الـ Promote
> ما تمّش، بادج «⚠️ الـ Worker نسخة قديمة» هيظهر في الهيدر — **مش تحذير
> تجميلي**: الواجهة بتبعت `results`/`machines`/`sortBy` وبتقرا حالة `already`،
> والـ Worker القديم بيتجاهلهم في صمت.

1. **Promote** بعد أول build، وبعدها افتح الأداة واضغط 🩺 في الإعدادات — لازم
   كل الفحوصات ✅. الفحص الجديد **`read_all_orders`** ممكن يطلع ❌ وده **مقبول
   دلوقتي** (راجع المسائل المفتوحة) — الباقي لازم يبقى ✅.
2. **اختبار نزع السلاح (أهم واحد):** استعلم عن أوردر واحد متوافق، اضغط "تحديث
   شوبيفاي"، وبعد ما يخلص **اتأكد إن**: الـ checkbox بقى مقفول · الزرار رجع
   رمادي · نافذة الملخّص فتحت · عمود "التحديث المطلوب" بقى "⏭ خلاص اتعمل".
   لو الزرار فضل شغّال بنفس العدد، الإصلاح الأساسي مش شغّال.
3. **اختبار "خلاص اتعمل":** استعلم عن أوردر حالته `S1 = Shipped` أصلاً — لازم
   يبان **رمادي** بجملة "الأوردر ده اتشحن خلاص"، **مش أحمر** وبرسالة تقنية.
4. **اختبار التقدّم:** دفعة ٢٠+ أوردر — الشريط لازم يتحرّك على مراحل والجدول
   يتلوّن أول بأول، مش يقفز من 0% لـ 100% في الآخر.
5. **اختبار السكانر:** اسكن باركودين ورا بعض بسرعة — لازم يدخلوا **رقمين
   منفصلين**، ولو اتلزقوا لازم تظهر رسالة "طوله N رقم" مش يتقبل الرقم الملزوق.
6. أول سكان فعلي: اتأكد إن الصف اللي فلفلمنته فشل بيبان **أصفر** مش أخضر.
7. **الـ CORS:** افتح الأداة من رابط GitHub Pages وتأكد إنها شغّالة. لو ظهر
   «تعذّر الوصول للـ Worker (شبكة أو CORS)» يبقى الأصل في `ALLOWED_ORIGINS`
   مش مطابق — راجعه حرف بحرف (بدون `/` في الآخر).

## استعلامات المتابعة بعد النشر

```sql
SELECT json_extract(extra,'$.result') AS res, COUNT(*) n, MAX(timestamp) last_ts FROM logs WHERE tool='metafields_change' AND extra LIKE '%"sourceTool":"bosta_orders_shipped_scanner"%' AND timestamp > '2026-09-07' GROUP BY res;
```

> المتوقّع بعد النشر: `success` بيزيد · `already` بيظهر (ده صحّي — دي حالات
> كانت بتتسجّل رفض) · `error` يفضل قريب من صفر. **لو `error` بدأ يزيد فجأة،
> ده مش عطل جديد بالضرورة — قارن `extra.stage`:** `write` معناها سباق حقيقي
> بين موظفين، و`lookup` معناها الحالة على شوبيفاي مش زي المتوقّع.

آخر تحديث: 07-09-2026 — 18:30

</div>
